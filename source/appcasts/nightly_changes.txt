2026-06-17: Turn on companionPairingAllowed

2026-06-17: Bump build to 3

2026-06-17: Add tools/tail_server_logs.sh to tail relay
Worker logs

2026-06-17: Rewrite companion relay design doc for clarity

Revise companion-relay-design.md section by section: drop
the unshipped Bonjour/LAN transport, lead with what the
relay is before implementation detail, define terms before
use, and check every value against the relay submodule and
client code. Add wire-level message diagrams for the pairing
and reconnect flows. Remove the low-value Cost model, Future
work, and Implementation sketch sections.

Fold the useful parts of companion-relay-dataflow.md into
the design doc (the at-rest storage inventory and the
per-location compromise summary) and delete it. Move the
push notification flow to its own companion-push-relay.md,
and strip the remaining push-relay detail from the relay
doc.

2026-06-17: Companion: iOS app, relay pairing, chat bridge,
and onboarding

Squash of the companion branch: the iTerm2 Companion iOS
app, the Cloudflare relay + App Attest pairing flow, the Mac
pairing/chat bridge, push notifications, and the What's New
onboarding links (companion, Claude Code, orchestration,
screenshot).

Full commit history is preserved on the companion branch.

2026-06-17: Remove support for Claude Opus 4 from AIMetadata

Deletes the claude-opus-4-0 model definition and its entry
in the model list, along with its now-orphaned refusal-shape
fixtures.

2026-06-15: Record llama text-cell attachment cassettes
(llama3.3) and enable on CI

Pin the llama attachment lane to llama3.3:latest: the
multimodal llama4 (67GB) does not fit in this 64GB machine
and is unusably slow (a single generation did not finish in
10 minutes, swapping at 22%/78% CPU/GPU).  llama3.3 fits and
loads in ~12s. It is text-only, so the lane records the six
text-shaped cells (plain/markdown/json/xml/svg/yaml); the
image and other binary cells are skipped rather than
recorded, since a text-only model can't process them. A
vision model that fits in RAM can light the image cells up
later.

Recorded the six cells against local Ollama; every cell
reported accept+probe with no matrix drift. Committed the
six cassettes and added LLAMA_API_KEY to the CI replay
config so the runner exercises the lane from cassettes
(verified: replays offline with a dummy key, 0 failures, no
network). This recovers 6 of the 16 previously-dead llama
skips as real round-trip coverage of the Ollama
serializer/parser path.

2026-06-15: Wire the llama attachment lane to local Ollama
(llama4:latest)

The llama lane was permanently stubbed to skip because there
was no hosted API to record cassettes against. Local Ollama
removes that reason, and the cassette layer is
vendor-agnostic (it intercepts at the AIPluginClient
transport chokepoint), so a recorded llama request/ response
replays on CI with no Ollama needed.

- resolveModel(.llama) returns llama4:latest (the multimodal
default,
  http://localhost:11434/api/chat) instead of throwing.
- The lane is gated on LLAMA_API_KEY: Ollama needs no real
key, but a
  non-empty value enables the lane and satisfies the
  driver's Registration. Unset (as in the CI replay config
  today) it still skips, so this does not change CI behavior
  until cassettes are recorded and the workflow opts in.
- run_ai_live.sh forwards LLAMA_API_KEY so a local record
run can enable
  it: LLAMA_API_KEY=ollama
  ITERM2_AI_LIVE_CASSETTE_MODE=record tools/run_ai_live.sh
  attachmentMatrix_llama
- The llama matrix cells are calibration placeholders (text
accepts the
  probe; binary cells guessed accept-but-garbled). Llama
  posts the chat-completions image_url shape to Ollama's
  native /api/chat, so whether the attachment is consumed is
  unknown until the first record run reports actuals;
  reconcile any drift then.

Verified the lane still skips (16/16) under the keyless CI
replay config, so the green build is unaffected.

2026-06-15: Tell claude not to be a dumbass

2026-06-15: Fix live filter dropping content delivered
mid-search

When a line was delivered to an AsyncFilter while a search
pass was already in progress (lastPosition == nil,
context.status == .Searching),
FilteringUpdater.didAppendToLineBuffer() left stopAt at the
value computed before the append. The in-flight
findSubstring therefore stopped at the stale boundary and
never examined the new line, so it was missing from the
filtered view until a later delivery re-triggered begin().
With the 60Hz cadence and 10ms time slice this window is
narrow in production, but under ASan (CI) a single search
pass can exceed the slice, making it reproducible:
AsyncFilterDeliveryQueueTests
testDeliveriesQueuedDuringCatchUp failed within ~15-20
iterations.

Recompute stopAt = computedStopAt on every append so an
in-progress search extends to cover newly delivered content.
For a bounded absLineRange (a selected command) this is a
no-op since computedStopAt stays pinned to the range
boundary, which is correct: content past the selected region
should not be filtered.

Adds tests/filter_midsearch_repro.sh for manual end-to-end
checking.

2026-06-14: Fix Session Status loss and harden workgroup
lifecycle

Session Status tool: - A per-tab status aggregate borrowed
the winning session's GUID and
  broadcast its clear() globally, so switching workgroup
  peers evicted the real session's still-live status from
  SessionStatusController.  The copyStatus() aggregate no
  longer broadcasts.
- Rows whose session can't be resolved render blank instead
of showing
  a recycled cell's content; per-reload session resolution
  is memoized.

Workgroup lifecycle: - Track instances by a stable
identifier so a workgroup whose leader
  already closed can still be exited and torn down instead
  of leaking.
- Guard mid-spawn teardown reentrancy and close stray spawns
so a
  member dying during entry can't leak shells or register a
  ghost.
- Refuse entry consistently across menu, triggers, and
controller
  through one predicate; restoration uses a counted multiset
  and a monotonic soft deadline.

Session lookup and reveal: - anySessionWithGUID also
searches buried peer ports and the workgroup
  registry so windowless peers resolve; reveal surfaces them
  by retrying the swap or reviving into a window.

Adds ModernTests coverage for the above.

2026-06-14: Add window.title variable for the rendered
window title

Holds the undecorated window title bar text (minus the
window-number prefix, bell, and transient size readout),
parallel to session.presentationName and tab.title. Updated
in -setWindowTitle and registered for variable-name
completion in the window context.

2026-06-14: Add a Variable Changed trigger match type

Fires when a named session-scope variable changes to a value
matching an optional regex. The evaluator registers an
iTermVariableReference observer per such trigger; the Add
Trigger UI gets a variable-name field (with completion) and
a value-regex field.

2026-06-12: improve code review system message

2026-06-11: Add AI-generated icons to the chat list

After the AI titles a chat, ask the configured model to draw
an SVG icon for the title, rasterize it (sandboxed-worker
validity gate plus in-process vector draw with the
fixedSVGData defs/use repair), and show it circle-clipped
next to the chat name. Chats without an icon show a default
chat-bubble icon. Forked chats inherit the source chat's
icon.

The prompt is editable in Settings > General > AI > Prompts
(Chat List Icon) and interpolates the title as
\(ai.subject); the Settings warning and help text cover the
new prompt. Prompt-template evaluation is unified in
AIPromptTemplateEvaluator (one-shot
iTermExpressionEvaluator, shared by Engage AI,
orchestration, and the icon prompt). One-shot AIConversation
retention is unified in completeOneShot, replacing the
per-site stored-property and static dictionary patterns, and
the rename flow no longer leaks its conversation on
asynchronous failure.

The icon is stored as a 128px PNG blob in the Chat table
(with migration), invalidated on title change, cached by the
cell view, and icon-only updates reload a single list row.

2026-06-10: Add companion icon to sketch file

2026-06-09: Add Reveal button and live window membership to
status-change alerts

The notify-on-status-change alert now offers a Reveal button
that jumps to the session whose status changed, shown only
when the session can still be resolved.

Replace the frozen per-window snapshot with a running
lastSeenStatusText baseline so a window watch covers every
session in the window, including ones created after it was
armed. Session scope keeps a one-shot arm-time baseline that
folds in any in-flight change so arming within a debounce
window does not fire spuriously.

2026-06-09: Wait for the page-saver test server to accept
connections

iTermTestHTTPServer launched python3 -m http.server and then
slept a fixed 1 second before returning. On a cold or loaded
CI runner the server can take longer than that to start
listening, so testBasicPageSaving (the first test to hit it)
failed with NSURLErrorCannotConnectToHost.  Poll the port
with short TCP connects until one succeeds (up to 15s) and
return as soon as it is accepting, instead of a fixed sleep.

2026-06-09: Fix dropped completion handler in
iTermAsyncStringConversion

The conversion is dispatched to a background queue from
init, but callers set completionHandler after init returns
(both VT100Parser and the unit test do). If the background
work finished before the handler was assigned, the handler
was never called: VT100Parser's sOutstandingPreconvertBytes
backpressure counter would never be decremented for that
conversion, and testAsyncCompletionHandlerCalled would time
out. The race was timing-dependent and only showed up on the
slower CI scheduler.

Deliver completion exactly once under the mutex, from
whichever side loses the race: the background completion
fires the handler if it is already set, and the setter fires
it if the work already finished. Also makes
completionHandler access thread-safe (previously read on the
background queue while written on the caller thread).

2026-06-09: Commit image attachment fixtures as static files

The imageHEIC matrix cells and the imageDescribe scenario
rendered their images at runtime. Those encoders are
host-dependent: HEIC's HEVC encoder varies by OS
build/architecture, and the imageDescribe PNG used
NSImage.lockFocus, which encodes at the screen's backing
scale (Retina dev machine vs headless runner). The bytes
therefore differed on the macos-26 CI runner from what the
cassettes were recorded with, so the canonical request hash
missed and replay (no network) failed with a provider error.

Freeze magic.png/heic/tiff and number.png as committed
fixtures, like magic.webp/pdf/zip already are, and load them
from disk. Generated on the recording host so they match the
existing cassettes (verified by replaying every image cell
offline: 0 failures, all served from cassettes), so no
re-recording was needed. Split the regeneration into
image-only and timestamped paths so refreshing the images
does not churn the pdf/zip timestamps and invalidate their
cassettes.

2026-06-09: Re-fit grid and scrollview when entering/leaving
instant replay

The instant replay session swaps in PTYTab set newView.frame
= oldView.frame and replace the subview, but when the frame
size is unchanged this does not fire
-resizeSubviewsWithOldSize:, so the incoming SessionView
keeps a stale scrollview frame. The synthetic and live
SessionViews can also differ in chrome (e.g. a per-pane
status bar), so the session grid and its scrollview height
end up inconsistent by a few lines.

That inconsistency produced two bugs:

1. While in instant replay, the scrollview was taller than
the grid, so
   -coordForPoint: mapped clicks a couple of lines below the
   cursor and dragging selected the wrong lines.
2. After leaving instant replay, the scrollview was shorter
than the
   grid, so the userScroll=NO bottommost-rect drawing
   disagreed with the userScroll=YES scroll-position
   drawing. The first mouse-down (which sets userScroll=YES)
   jumped the content by a few lines and revealed a stale
   history line.

Fix both swap paths to run the same re-fit the peer-swap
path (-sessionActivateSession:) already does:
fitSessionToCurrentViewSize: (guarded for tmux) plus
-layoutContentsForNewlyActiveSession. On enter this happens
in -setDvrInSession: before -setDvr: so the synthetic grid
matches its view before the first frame; -setDvrFrame then
resizes to the recorded frame size. On exit it happens after
the live view is swapped back in.

Also adds DLogs at the swap points and in -coordForPoint: to
make the grid-vs-scrollview relationship visible in debug
logs.

2026-06-09: Fix FakeSession conformance after Exit Workgroup
leaderOnly rename

142d3d6af renamed the delegate method to
triggerSideEffectExitWorkgroupLeaderOnly: but updated the
FakeSession test mock to
triggerSideEffectExitWorkgroup(leaderOnly:), whose ObjC
selector differs, so FakeSession no longer satisfied
iTermTriggerSideEffectExecutor and the ModernTests target
failed to compile. The app targets build without the test
target, so it slipped through locally, and CI was masking it
via the xcodebuild exit-code bug. Rename the mock method to
match the protocol.

2026-06-09: Rebuild binary deps with Xcode 26.5; run CI on
macOS 26 with 26.5

The committed prebuilt dependencies were rebuilt from clean
with Xcode 26.5 (17F42), and last-xcode-version updated to
match. The build phase gate compares xcodebuild -version
against last-xcode-version byte for byte, so CI has to use
the same toolchain.

GitHub's macos-15 image tops out at Xcode 26.3, so switch
the xcode-tests job to the macos-26 runner and select Xcode
26.5, which matches the toolchain the deps were built with.

2026-06-09: Make Exit Workgroup teardown leader-only and
job-ended detection event-driven

Reloading or killing claude in a Claude Code workgroup peer
(Code Review or Diff) tore down the whole workgroup: peers
inherit the leader's profile, which carries the claude-code
"Job Ended: claude" Exit Workgroup trigger, so a peer's
claude ending fired it.

- Add a leaderOnly option to ExitWorkgroupTrigger (stored in
eventParams,
  surfaced via a new Trigger.hasLeaderOnlyOption property).
  When set, the trigger only tears down the workgroup if it
  fired on the leader (main) session. The installer sets it;
  a hand-made trigger defaults to the legacy
  fire-from-any-session behavior.
- Surface it as a checkbox in the trigger editor, shown for
any match
  type (so a regex-based Exit Workgroup trigger can toggle
  it too), and collapse the empty parameter row for
  parameter-less triggers.
- Drive job started/ended from an iTermProcessCache
foreground-ancestry
  notification instead of inferring it from the periodic
  title poll, so a job's termination is detected reliably
  even when it dies together with the session (the poll
  froze in that case). The cache emits a final
  empty-ancestry change on unregister. The session matches
  the effective tracked pid (tmux client pid for tmux
  sessions, else the shell pid).
- Migrate existing installs: on upgrade from a
pre-leaderOnly build
  (<= 3.7.0beta3 or the 2026-06-08 nightly) set leaderOnly
  in place on existing claude Exit Workgroup triggers, once,
  on the version transition. Reinstalling also replaces an
  older flagless trigger.

2026-06-08: CI: pin Xcode to 26.4 and unmask xcodebuild exit
code

The Run tests step captured $? after piping xcodebuild
through tee, which always exits 0, so a failing build or
test run reported as a pass. Use ${PIPESTATUS[0]} to read
xcodebuild's real exit code.

Also bump the selected Xcode from 26.2 to 26.4 so CI matches
the version the committed prebuilt binary dependencies were
built with; otherwise the dependency build phase regenerates
them and fails.

2026-06-08: AI live test cassettes: recordings,
deterministic fixtures, and CI

Test-side work for the cassette record/playback layer (the
production seam landed separately). All offline; no
production code.

- Record the full cassette set
(ModernTests/Resources/AICassettes) so the AI
  live suite replays for free; the whole suite passes
  offline.
- Recorder skips cancelled requests so a slow-model timeout
can't poison a
  cassette.
- Make the PDF and ZIP attachment fixtures deterministic:
commit them static
  (loaded via loadDiskFixture) instead of generating per
  run, which embedded a timestamp (PDF /CreationDate, ZIP
  mtime) and defeated caching. Generators stay behind an
  opt-in regeneration test.
- Make the chat-queue tests cacheable: a fixed,
still-unguessable tool-output
  marker instead of a random one, plus the one stray request
  replay's instant timing produces.
- Fix AISafetyRefusalParserTests /
AIMetadataFixtureCoverageTest: timed-out
  captures were being written as refusal fixtures and
  choking the parser. The capture now refuses to write a
  failed request, the parser test skips any stragglers,
  gemini-3.5-flash was re-captured, and gpt-5.5-pro (blocked
  at HTTP 400 by OpenAI's cyber_policy filter) is exempted
  from coverage.
- CI: write a replay-mode config before the test run so the
harness replays
  the committed cassettes instead of skipping itself.

2026-06-08: Persist auto-approved tool requests instead of
squelching them

Auto-approved (.always) and auto-denied (.never) tool calls
dropped the
.remoteCommandRequest from the chat DB and kept only the response, so the
next turn rebuilt a history with an orphan tool_result (a
tool_result with no matching tool_use) that vendors 400.
This has been the behavior since the original AI Chat
commit; translate's orphan synthesis (added later) healed it
only on the next turn and was one-directional.

Fix the root cause: persist the request.

- ChatClient.processRemoteCommandRequest (.always/.never)
now appends the
  request to listModel before running/denying the tool, then
  still returns nil so it is not broker-delivered to the
  live UI. History is complete going forward; no new
  orphans.
- ChatViewControllerModel hides a .remoteCommandRequest
whose matching
  .remoteCommandResponse exists (resolvedRequestIDs), so the
  persisted request never shows a stray Allow/Deny prompt.
  Live UI is unchanged (auto requests are never
  broker-delivered); this also drops stale buttons on
  reloaded .ask requests. No Message.Content change
  (synthesized Codable would break existing chat DBs), so
  old data decodes untouched.

Tests: - ChatRestoreToolUsePersistenceTests: restores
(Codable round-trip) a chat
  DB from three eras and asserts the rebuilt LLM prompt is
  valid with no duplicate synthesis: old (orphan-only),
  mixed (old orphans + new pairs), future (all persisted
  pairs). Plus the view-model hide rule.
- AILiveHarness: a live orphan negative control. The
positive
  chatReload_orphan test proves a vendor ACCEPTS a repaired
  orphan; the negative control sends the same orphan WITHOUT
  repair (ChatAgent.transcriptMessagesBeforeRepair) and
  asserts the vendor REJECTS it with HTTP 400. Together they
  prove the repair is necessary and sufficient, so the
  positive test can't pass by accident. Verified live:
  Anthropic and OpenAI 400 the un-repaired orphan; both
  accept the repaired one.
- Retire
test_chat_squelchedToolUseHistoryDoesNotBreakNextTurn and
its
  SquelchingToolProcessor: it verified orphan repair through
  a multi-turn chat where the model nondeterministically
  re-called the tool (flaky), and its coverage is now served
  deterministically by the restore tests and the live
  positive/negative pair.

2026-06-08: Add record/playback cassettes for AI live tests;
fix chat streaming threading

The live AI harness hit real vendor APIs on every run. Add a
cassette layer that records request/response pairs and
replays them offline so the suite can run for free once
recorded.

- iTermAIClient gains requestInterceptor (replay) and
responseRecorder
  (record) hooks at the single network chokepoint; nothing
  in the shipping app sets them.
- AICassette.swift canonicalizes each request (redacts API
keys, maps
  UUIDs and the multipart boundary to stable tokens, sorts
  JSON keys) so byte-identical logical requests hit the same
  cassette. Errors delivered as streamed content (e.g. an
  OpenAI quota error mid-stream) are not recorded, so they
  cannot poison a cassette.
- Modes off/auto/replay/record via
ITERM2_AI_LIVE_CASSETTE_MODE; each
  request logs HIT/MISS/RECORD so it is clear whether a
  value came from cache or the wire.
- The cassette session installs in setUpWithError so
chat-queue tests that
  bypass AILiveDriver are covered too.

Also fixes a real streaming bug that replay surfaced:
AITermController delivered stream .word events on
iTermAIClient's background queue, but they drive the
@MainActor chat stack (ChatService to ChatBroker to SQLite).
Tight replay delivery tripped SQLite's multi-threaded access
guard every time; live only raced occasionally. Hop .word to
main like the begin path already does.

And treat an exact HTTP 400 as a valid refusal in the
refusal scenario (OpenAI's gpt-5.5 family blocks the
phishing prompt at the API layer instead of refusing
in-band), keyed strictly on the status code via a shared
AILiveHarness.httpStatusCode helper.

Recorded cassette data is kept locally and not committed
yet.

2026-06-08: Add @-mention of terminal sessions in
orchestration chats

Typing a word beginning with @ in an orchestration-mode AI
chat opens a picker listing current sessions in a window >
tab > pane > peer outline (peer level omitted when a pane
has no peers), with a live preview of the highlighted
session and a colored overlay on its real view. Choosing one
inserts an atomic token that displays as the session name
but serializes to @<session-guid>, matching the form the
orchestrator emits and OrchestrationMentionRenderer parses.

Details: - Extract the Open Quickly session preview into a
reusable
  iTermSessionPreviewPanel, shared by Open Quickly and the
  picker.
- Mentions render consistently across the picker, the
inserted token, and
  message bubbles via a single ChatMentionDisplay helper:
  workgroup sessions are prefixed with their component role
  (e.g. "Diff", "Code Review"), with the colon omitted when
  the session has no title yet.
- Peers list in the same order as the peer switcher control.
- Window/tab rows show their activation shortcut
(configurable modifiers).  - Mention rewriting is gated to
orchestration chats so ordinary chats
  never mutate user text containing an @<uuid>-looking
  substring.
- The picker placeholder advertises the feature.  - Per-chat
drafts: switching chats clears the field but restores
pending
  (unsent) input, tokens and attachments included.

2026-06-07: Centralize notify-on-status-change and surface
it in the Cockpit

Move per-window notify-on-status-change out of the Session
Status toolbelt tool into a new
iTermNotifyOnStatusChangeController so it works regardless
of toolbelt visibility, and add a per-session scope on top
of it. One-shot semantics are preserved: a watched session
changing status shows a single alert and disarms. A change
that satisfies both a session watch and its window watch
consumes both but alerts once.

- ToolStatus bell now toggles the centralized state for the
selected
  session, or the window when nothing is selected, and shows
  a per-row bell indicator for armed sessions.
- The Window menu routes through the controller by terminal
guid.  - The Cockpit gains a bell toolbar item that arms the
selected window or
  session, with an accent-colored bell shown at the leading
  edge of armed rows.
- The Cockpit panel uses the utility-window style so it no
longer dims
  distractingly when it loses key status.

2026-06-07: Show session detail under name in Cockpit
group-by-status mode

In group-by-status mode, session rows now render their live
detail string (OSC 21337 detail=...) on a second line under
the session name, in a smaller, dimmer font. The detail is
markdown, so it is rendered with the shared
AttributedStringForGPTMarkdown renderer and scaled down to
the detail size. Rows without a detail stay single-line and
unchanged. The diff path reloads and recomputes row height
when a session publishes or clears its detail.

2026-06-07: cc-status: only re-deploy the symlink when
launching a newer version

The launch-time refresh re-pointed the cc-status symlink at
the running app's bundle binary unconditionally, so
launching an older build would downgrade the deployed
cc-status. Gate the refresh on the running version being
newer than the previous launch (compared with the canonical
SUStandardVersionComparator via -[iTermApplicationDelegate
version:newerThan:], which honors minor/build components),
while still repairing a missing or broken symlink on any
launch.

Adds ccStatusSymlinkIsHealthy() for the repair-anyway branch
and a private declaration of version:newerThan: so the
earlier call site sees the correct BOOL return type.

2026-06-07: Update menu item shortcuts spreadsheet

2026-06-07: Add Notify on Status Change to Session Status
tool

Add a bell toggle to the bottom-left of the Session Status
toolbelt tool. When armed, the next net change to any
visible session's status text (waiting/idle/busy, not
detail) shows an NSAlert and disarms itself. The transition
is evaluated on the coalesced debounce flush against the
controller's current status text, so a brief flicker that
returns to the armed-time value does not alert.

Also expose the same per-window toggle as Window > Notify on
Status Change (shift-cmd-X), with a menu icon and help tip.

2026-06-07: cc-status: humanize unknown tool names and
extract better detail

The PermissionRequest detail fell through to the raw tool
identifier for any tool without an explicit case, leaking
internal keywords like "AskUserQuestion" into the UI as
"Allow AskUserQuestion?".

- Humanize unknown tool names (AskUserQuestion -> Ask User
Question) so a
  raw keyword never reaches the UI.
- AskUserQuestion and ExitPlanMode are not permissions to
grant; show the
  question text / "Review proposed plan?" instead of "Allow
  ...?".
- Extract the identifying field for MultiEdit (file_path),
NotebookEdit
  (notebook_path), and Task (alias of Agent, description).

2026-06-07: Move +/- above checkboxes and add tooltip to
Merge workgroup statuses

In the Session Status settings popover, reorder the layout
so the +/- segmented control sits above the checkboxes
(between the table and the two toggles). Add a tooltip to
the Merge workgroup statuses checkbox explaining that only
the most recent status from each workgroup is shown.

2026-06-07: Show suite name in Settings window title

When launched with -suite <name>, the Settings window title
now reads "Settings: <name>" instead of just "Settings".

2026-06-06: Add option to merge workgroup statuses in the
Session Status tool

When enabled, each workgroup collapses to a single row whose
representative is the highest-priority status, with ties
broken by the most recent update. Solo sessions remain
individual rows. When disabled, the prior per-session
behavior is preserved.

The Session Status model is now split into the full
per-session list and a table-facing displayedStatuses that
the merge rebuilds.  Selecting the active session also maps
through the workgroup so the merged row highlights when any
peer is focused.

2026-06-06: Populate Jobs toolbelt immediately for restored
sessions

The Jobs toolbelt was empty after restoring a session and
only filled in once the next job launched or a shell prompt
arrived. The tool roots its tree at the session's
effectiveRootPid variable, which is set by
setCurrentForegroundJobProcessInfo: and only when a
foreground-job recompute runs. A reattach during restoration
never spawned the shell through the normal launch path, so
nothing recomputed the foreground job: the variable stayed
unset and sessionProcessInfoProviderDidChange: never fired,
so the toolbelt was never refreshed with the live pid.

taskDidRegister: (the unified main-thread callback fired by
every job manager once the pid is live, on both fork and
attach) now calls updateTitles, which recomputes the
foreground job, establishes effectiveRootPid, and refreshes
the toolbelt right away.

2026-06-06: Fix Fold All and Clear Buffer leaving portholes
behind

Inline views (portholes, e.g. imgcat output) were left on
screen after Fold All and Clear Buffer.

Fold/unfold: folding a region containing a porthole now
hides its view via intervalTreeDidHideObject and unfold
restores it via intervalTreeDidUnhideObject, scoped to the
fold reason so porthole add and resize (which share
reallyReplaceRange:) are unaffected.

prunePortholes: now keys porthole-view lifetime off whether
the mark still occupies a valid location in the interval
tree (coordRange), not off mark deallocation. The progenitor
PortholeMark outlives its removal via its doppelganger, so
the old registry-nil predicate never fired for Clear Buffer.
Pruning also drops the registry references to break that
retain cycle and avoid leaking the porthole and its saved
lines.

Permanent fold removal: a fold destroyed without unfolding
(Clear Buffer or scrollback overflow) carries away portholes
stashed in its savedITOs.  A new
intervalTreeDidPermanentlyRemoveHiddenObject signal reclaims
them, fired only from the genuinely-permanent removal sites
(removeObjectsFromIntervalTree: and
removeInaccessibleIntervalTreeObjects) rather than the
shared didRemoveObjectFromIntervalTree: hook, which is also
used for temporary remove and re-add patterns.
forciblyRemovePorthole drops the registry references
unconditionally so a never-hydrated porthole (restored
already folded, then cleared) is not leaked.

Also suppress Core Animation's implicit order-out fade when
a porthole is removed as part of folding or clearing, so it
disappears instantly like the surrounding text.

Adds PortholeFoldClearTests covering the fold/unfold/clear
signalling.

2026-06-06: Fix IntervalTreeGraphEncodingTests compile break

VT100RemoteHost's initWithUsername:hostname: is inside
NS_ASSUME_NONNULL and returns a non-optional, but these
tests bound it with `guard let`, so the file never compiled
and blocked the whole ModernTests target. Convert the
optional bindings to plain `let`.

2026-06-06: Add a process inspector popover to the Jobs view

In the Jobs view (toolbelt and status bar popover):

- Hovering a row shows the full, untruncated command
  as a tooltip.
- Pressing space, or clicking a new inspect button
  (magnifyingglass) in the bottom-right corner, opens an
  inspector popover for the selected process.

The popover shows the full command, working directory
(fetched asynchronously), process ID, start time (which
ticks while open), the environment (sorted by variable
name), and a table of open files and network connections.
Command, working directory, and process ID each have a copy
button that shows a Copied toast.

Environment and file descriptors are parsed from
KERN_PROCARGS2 and PROC_PIDLISTFDS/proc_pidfdinfo via new
iTermLSOF methods (environmentForProcess: and
fileDescriptorsForProcess:, the latter decoding socket
protocol, addresses, ports, and TCP state).

2026-06-06: Fix next/previous pane while maximized after
restarting a session

While a pane is maximized, -orderedSessions builds the
reading-order list from a snapshot captured at maximize
time. That snapshot was keyed by session GUID, but Session >
Restart Session assigns the same session and SessionView a
fresh GUID (see -[PTYSession
replaceTerminatedShellWithNewInstance]). After a restart the
snapshot's old GUID no longer resolved, the active session
fell out of orderedSessions, -sessionInDirection: returned
nil, and Next/Previous Pane (cmd-[ / cmd-]) silently did
nothing.

Key the snapshot by idMap_ keys instead of GUIDs. idMap_ is
the structure every while-maximized mutation already keeps
current (peer swaps, and synthetic-session teardown in
-showLiveSession:inPlaceOf:), so resolving the order through
it survives a pane getting a new GUID or a new SessionView
without the snapshot drifting out of date. This also fixes
the analogous breakage when exiting a synthetic session
(instant replay, filter, screenshot) that was active when
the tab was maximized. Reading order is preserved; the MRU
branch is untouched.

Add a -sessionInDirection: canary log for any future drift.

2026-06-05: Fix porthole copy and click-to-activate

Copy from a porthole declared an RTF pasteboard type even
when writing plain text, so RTF-preferring paste targets
received empty data. Declare the content type that matches
what each mode writes.

Clicking a porthole in an inactive split pane did not
activate the pane because the porthole text view refuses
first responder. Forward its mouseDown to make the
containing PTYTextView first responder.

2026-06-05: Add session restoration and process reattachment
doc

Document how iTerm2 restores windows at launch and
reconnects restored sessions to surviving multiserver daemon
children via partial attachments, including the two-phase
attach, the timeout/orphan-adoption fallback, the
non-restoration partialAttachments:nil path, and how this
interacts with macOS NSWindowRestoration (GUID handshake,
completion-block placement, and the fullscreen deferral).

2026-06-05: Persist and restore workgroup peers across app
restart

Workgroup peer-group members (Diff, Code Review) were
runtime-only and were never saved, so an app restart (e.g. a
Sparkle upgrade) lost them: peers are born buried and fail
to register with iTermBuriedSessions, and the peer port /
instance live only in memory.

Persist the workgroup as an opaque descriptor on the visible
(anchor) member's arrangement, embedding the other (buried)
members' arrangements and recording non-peer split/tab
children by GUID. On relaunch, a coordinator rebuilds the
workgroup by adopting the already-restored sessions
(reattaching to their live servers when present) instead of
spawning fresh ones.

Details: - PTYSession owns a single opaque
SESSION_ARRANGEMENT_WORKGROUP key; all
  schema lives behind iTermWorkgroupRestoration, with a
  recursion guard so embedded members don't re-embed.
- WorkgroupRestorationCoordinator gathers anchors during
restore and
  reconstructs each once its window is ready; idempotent and
  re-armed from the restorable-state-controller completion,
  the post-restoration block, and
  iTermDidDecodeWindowRestorableState.
- iTermWorkgroupInstance.adopt is a pure-wiring sibling of
enter that
  reuses a shared assemble helper; peers without a restored
  session (not-yet-started deferred peers) are spawned fresh
  so they come back with their Code Review / Diff overlay
  rather than a stray shell. The anchor's overlay is
  re-presented when it was pending.
- Reconstruct runs before orphan-server adoption so live
peers are
  reattached, not pulled into stray windows.
- iTermWorkgroupController resolves any member session to
its leader so
  Exit Workgroup works when a non-leader peer is focused.
- The Enter Workgroup trigger no-ops while a session is
being restored
  into a workgroup, to avoid spawning a duplicate.

Adds WorkgroupRestorationTests covering adopt wiring,
idempotency, anchor != leader, non-peer children,
spawn-fresh for not-started peers, pending-flag encoding,
trigger suppression, and the recursion guard.

2026-06-05: Add a help button to the Workgroups settings
pane

- Add a circular "?" help button in the bottom-right of the
  Workgroups editor that opens the Workgroups documentation
  via the canonical (version-agnostic) URL.
- Align the detail pane's bottom with the table so the
button
  sits in a clear bottom strip.
- Show the "?" button only when a workgroup is selected,
since
  the empty state already has its own "What is a Workgroup?"
  button. The two are now mutually exclusive.
- Shorten the "What is a Workgroup?" popover to a single
  sentence that links to the full documentation.

2026-06-05: Fix all build warnings

Compiler (Swift + ObjC): - Remove no-op as? downcasts and
unnecessary ?? on non-optionals - var->let, missing switch
case, unused var - Break a retain cycle, make implicit-self
explicit, add @dynamic
  for inherited/forwarded properties, add missing protocol
  stubs
- (void)-cast a warn_unused_result call

Nullability: - Annotate VT100TerminalDelegate,
VT100RemoteHost, iTermKeyMappings.
  VT100TerminalDelegate nullability was driven by a full
  audit of the implementation and every call site, tracing
  ObjC nil-propagation (token.string/.savedData/.subtokens,
  dataUsingEncoding:, dict[key], stringForAsciiData, and
  nullable receivers like self.config).

Resilient-coordinate protocol conformances: - Move
Swift-@objc-protocol conformances out of
forward-declaration-only
  public headers into class extensions / a new
  VT100ScreenMutableState+RCDataSource category, mirroring
  the existing VT100ScreenState+RCDataSource pattern. Kills
  the repeated "cannot find protocol definition" warnings.

Global search RC data source: -
iTermGlobalSearchEngineCursor passed a non-conforming
PTYSession* as the
  ResilientCoordinate data source (would hit an unrecognized
  selector on rcGuid). Add
  VT100Screen.resilientCoordinateDataSource (the main-thread
  RC pool) and use it.

AI-chat Swift concurrency: - nonisolated(unsafe) for
deinit/main-hop captures, nonisolated static let
  for sentinel strings, MainActor.assumeIsolated for on-main
  UI closures, @preconcurrency on the
  MessagePrepPipelineDelegate conformance.

MainMenu.xib: - Give the Exit Workgroup / Next Peer /
Previous Peer menu items their own
  identifiers (they were copy-pasted duplicates) and add
  matching entries to MainMenuMangler's icon map.

2026-06-05: Git diff menu: exclude untracked files from
per-file statuses

The workgroup code-review/diff feature requests git state
with includeDiffStats=YES, which enabled rename detection in
the service-side status walk. Combined with
INCLUDE_UNTRACKED, libgit2 hashed the contents of every
untracked file in the tree for rename similarity. In a
working copy littered with large untracked files this
exceeded the git timeout, so the poll never returned: the
diff toolbar stayed empty and the diff peer hung on "Diff
session is waiting for changes".

Split the single status_list pass into two: a count pass
(untracked included, no rename detection) for
dirty/adds/deletes, and a separate fileStatuses pass
(untracked excluded, rename detection over tracked files
only) for the diff menu. The diff menu already filtered
untracked entries out of its display, so dropping them at
the source changes no visible behavior there while
eliminating the expensive hashing.

Because untracked files no longer reach the menu, they no
longer constrain the shared-directory prefix used to shorten
displayed paths; labels for tracked changes now shorten
against only the tracked files, which is more predictable
than depending on incidental untracked clutter. Updated the
now-stale comment in CCDiffSelectorItem and extracted the
prefix/shortening logic into testable helpers.

The status bar component is unaffected: it uses
includeDiffStats=NO (only the workgroup sets it YES) and
reads dirty/branch/ahead, not fileStatuses, so its code path
and counts are unchanged.

Adds integration tests that build real git repos in temp
dirs and read them back through iTermGitClient, plus unit
tests pinning the diff menu's path-shortening behavior.

2026-06-05: Code review overlay: default to last-used prompt
on reload

When a code-review-mode session re-presents the prompt
overlay (toolbar reload or broken-pipe restart), default to
the prompt the user last submitted, whether a preset or a
hand-edited value, instead of reverting to the store's
last-selected preset.

Adds a per-session codeReviewLastUsedPrompt recorded on
submit and used as the overlay default when present; the
first presentation still falls back to
CodeReviewPromptStore.defaultPromptText.

2026-06-04: Workgroup peer switcher: spin an activity
indicator while a peer works

NSSegmentedControl draws segments through NSSegmentedCell
rather than hosting per-segment subviews, so there is no
view whose frame can host an NSProgressIndicator.
WorkgroupPeerSegmentedCell overrides
drawSegment(_:inFrame:with:) to capture the exact rect
AppKit computes for each segment;
WorkgroupPeerSegmentedControl uses that rect to lay a
PSMProgressIndicator over the segment's leading edge. A
transparent reserved image keeps the label from sitting
under the spinner.

iTermWorkgroupPeerPort observes iTermSessionTabStatus
changes, maps the session GUID to its peer identifier, and
fans the busy state across every peer's switcher (all
switchers mirror the full peer set). The working
determination reuses
WorkgroupIntrospection.state(forTabStatus:) so the
definition of working stays in one place; seedBusyStates()
catches peers already working at construction time.

2026-06-04: Cockpit: watch statusless sessions by screen
observation

register_watch previously relied entirely on tab-status (OSC
21337 / cc-status) transitions. Sessions that report no
machine-readable status (plain TUIs, coding agents like
Codex that don't emit status, bare shells) produced no
transitions, so the orchestrator had no event channel and
was reduced to polling get_screen_contents in a loop,
junking the chat with "still busy, checking again" turns.

When the target session reports no status, doRegisterWatch
now creates a
.screenPoll watcher driven by ScreenWatchPoller: a headless
AIConversation that reads the rendered screen, judges
doneness from on-screen content (activity indicator vs ready
prompt, not screen stability, since a silently churning
agent leaves the screen static), and fires the same
status_update a tab-status watcher would. The agent's turn
still ends immediately; no polling noise reaches the chat.

Details: - Screen stability is not doneness: the model is
handed the first
  capture plus the two most recent so it can see whether an
  indicator is still animating across frames.
- Per-target detection: idle/waiting/working each spell out
their own
  positive on-screen evidence (an indicator means reached
  for working but not-yet for idle), instead of one
  finished-centric description.
- Unparseable replies get one rephrase pass, then fall back
to unknown,
  which the loop treats like not-yet so it never fires a
  false positive.
- Quadratic backoff delay(n)=3+n*n between polls, hard
5-minute cap,
  then a watchTimedOut status_update so the agent can
  re-register.
- Watchers carry a mode; .screenPoll pollers are cancelled
on
  unregister/terminate/teardown and restarted after
  relaunch. The tab-status handler is scoped to .tabStatus
  watchers.
- Poll lifecycle and model verdicts are traced via DLog.

2026-06-04: Improve Claude Code setup wizard navigation and
highlight

- Show Toolbelt is now skippable: Next is always enabled on
that
  step, while Show remains the default button until the
  toolbelt has been shown, after which Next becomes the
  default.
- The Before You Start sheet has an always-visible link to
  https://iterm2.com/claude-code-integration.html below the
  "What gets changed" disclosure row, regardless of its
  state.
- Fix the Session Status highlight not appearing when the
toolbelt
  is shown fresh. OnboardingScrim computed its cutout once
  in draw() and never re-evaluated, so when the Session
  Status tool's wrapper got its real frame on a layout pass
  after the scrim's first draw, the highlight was punched
  out of a zero-sized rect.  The scrim now observes its
  cutout view and the toolbelt for frame changes and
  redraws, mirroring iTermPrefsScrim.
- LinkButton gains configureLinkAppearance() so it can be
styled
  when built programmatically (awakeFromNib isn't called
  then).

2026-06-04: AI chat orchestration: render tool-call activity
lines via the @-mention path

The tool-call activity line ("Reading screen of ...")
rendered session and workgroup targets through
OrchestrationToolProvider.sessionDescription, a separate
path from the @-mention link machinery. That meant
references were plain bold text rather than clickable links,
and a target that had just closed leaked a raw GUID
("session BAA338E7-...") instead of the "[defunct session]"
the mention renderer produces.

Have the activity-line builder emit @<session_guid> /
@<workgroup_id> tokens and run the rendered external payload
through OrchestrationMentionRenderer, so these references
resolve at display time to a clickable current-name link or
"[defunct session]", sharing the same machinery the
orchestrator's text bubbles already use.

2026-06-04: Orchestration: auto-approve safe session spawns

In orchestration mode, start_session previously always
prompted the user for permission. Now a spawn with no
command is allowed without prompting, and a spawn with a
command is vetted by the auto-mode safety classifier
(CommandSafetyChecker): an unambiguous "safe" verdict
auto-approves, and anything else (block, needs-manual-
approval, unparseable, or a classifier error, all
fail-closed) falls back to the existing inline user prompt.

2026-06-04: AI chat: offer orchestration when no session to
link

A new chat created without a current session previously
showed no follow-up offer. Now it publishes an
offerOrchestration client-local bubble (parallel to the
existing offerLink bubble) with a single Enable
Orchestration button routed through the same confirmation
alert as the menu-driven toggle.

2026-06-04: AI timeout: bump default from 30s to 5 minutes

Slow models and long responses frequently exceeded the old
30-second default. New installs now get a 300-second
timeout.  Existing users who already have a stored value
keep it.

2026-06-04: Claude Code integration: flush profile model
after trigger install/uninstall

Installing the Claude Code integration mutated the shared
ProfileModel via setObject:forKey:inBookmark:, which updates
the in-memory model and posts reload notifications but never
writes back to user defaults. The settings UI reflected the
new triggers, but they were lost on the next launch.
Uninstall had the mirror bug.

Call ProfileModel.flush() after the shared-model writes in
both paths, matching what every settings-UI trigger editor
already does. flush also correctly skips persistence when
New Bookmarks was overridden to a file on the command line.

2026-06-04: AI command safety: migrate existing users, keep
on-device option

Default the safety-check side-query to the configured
conversation model, but grandfather users who already
enabled it under free on-device Apple Intelligence. A
one-time migration keeps them on-device, but only when Apple
Intelligence is actually working on this Mac (so a checkbox
that was a no-op on incapable hardware is not preserved as
on-device), and flags them to be asked, before the next
checked command, whether to switch to their possibly-paid
model. Declining keeps Apple Intelligence. The on-device
path fails closed rather than silently falling back to a
cloud provider, and Macs not running Apple Intelligence are
re-prompted instead of grandfathered.

Hoist the NoSync routing flags into shared, compiler-checked
constants so a typo cannot silently mis-route this privacy
boundary. Replace stale Apple-Intelligence user-facing copy
(the approval warning and the settings checkbox) with
provider-neutral wording. Add a force parameter to
RemoteCommand.isSafe that makes the check mandatory for
orchestration chats.

2026-06-04: Orchestrator: address sessions by session_guid,
not workgroup+role

Session-acting orchestrator tools (get_state,
get_screen_contents, send_text, interrupt,
start_code_review, register_watch) took a nested target
object {workgroup_id, role}. Models stumble on emitting the
nested shape and sometimes leak Claude's internal <parameter
name=...> tool-call markup into the JSON string value,
producing a malformed target that the dispatcher rejects
with malformed_args.

A session_guid is globally unique, is surfaced on every role
in the <workgroups> snapshot, and is the same way the
session_* tool family already addresses sessions. Switch
these tools to take a single session_guid string and derive
the owning workgroup and role app-side via the existing
context(for:) reverse-resolver.

- WorkgroupIntrospection: add resolve(sessionGuid:) and
  claimScope(forSessionGuid:); remove resolve(target:) and
  availableRoleNames.
- OrchestratorCommand: drop OrchestratorTarget; args and the
  getState/interrupt cases carry session_guid; replace
  requiredWorkgroupClaim with a ClaimRequirement enum so
  session writes resolve to their workgroup scope
  (preserving workgroup-granularity approval) while clipping
  tools still claim by workgroup_id; startedSession returns
  session_guid.
- OrchestratorDispatcher: decode session_guid; gating
resolves
  session to scope and throws unknown_session for a dead
  GUID; resolveSessionOrThrow replaces resolveTargetOrThrow;
  start_session returns the GUID.
- OrchestratorError: add unknownSession(guid:);
targetNoLongerExists
  carries a GUID; remove the now-dead unknownRole.
- Tool schemas: one shared session_guid field across the six
tools.  - Activity-line rendering reads session_guid; drop
the leaked-XML
  salvage path that existed only to cope with the old
  malformation.
- System prompt: add an "Addressing sessions" section and
repoint the
  driving/watching/code-review guidance at session_guid.

2026-06-04: AI chat: render @-prefixed session/workgroup ids
as name links

In orchestration mode the agent now writes a session_guid or
workgroup_id prefixed with "@" when it wants to point the
user at a specific session or workgroup. When rendering an
agent message, OrchestrationMentionRenderer replaces each
"@<id>" with a clickable link, marked by a terminal icon,
that shows the entity's current name and reveals the session
on click. Ids that no longer resolve render as "[defunct
session]".

The renderer's transform is split from the live lookup so it
can be unit tested with an injected resolver.

2026-06-04: Disable automatic text substitutions in AI chat
input field

2026-06-04: Fix several low-risk crashes seen in 3.6.10
reports

- iTermHotKeyProfileBindingController: skip profiles with no
GUID
  before -[NSMutableSet removeObject:], which raises on nil.
- SSHProcessInfoProvider: guard the shell-command components
with
  .first instead of subscripting [0], which traps on an
  empty command from a remote ps row.
- ProfileModel: use [NSString castFrom:] for the
default-bookmark
  value so a legacy boolean can't get -isEqualToString:.
- iTermTextDrawingHelper: fall back to the default color
when a
  background run's resolved color is nil while drawing
  margins.
- iTermMetalView: skip fetching a drawable when the drawable
size
  has a zero dimension, avoiding a divide-by-zero in
  QuartzCore.
- iTermPresentationController: detect the invalid
presentation
  option combination macOS exposes transiently during a
  Spaces transition and defer the update (bounded retry)
  instead of setting it, rather than letting AppKit raise.

2026-06-04: Add latest AI models and omit temperature for
Opus 4.7+

Add GPT-5.5 and GPT-5.5 Pro, Claude Opus 4.8, Opus 4.7, and
Sonnet 4.6, and Gemini 3.5 Flash and Gemini 3.1 Pro. Repoint
the default and recommended models to GPT-5.5, Claude Opus
4.8, and Gemini 3.5 Flash.

Opus 4.7 and later reject the deprecated `temperature`
request parameter with HTTP 400, so add a
supportsTemperature flag to AIMetadata.Model (defaulting to
true) and have the Anthropic request builder omit
temperature when it is false. Set it false on Opus 4.8 and
Opus 4.7.

Captured refusal fixtures for the seven new models and
verified smoke, refusal, and tool-call round-trips against
the live vendor APIs.

2026-06-03: AI command safety: quote-aware hard rules and
provider-agnostic classifier

Rework the agent command safety check so it no longer
depends on Apple Intelligence or macOS 26.

TerminalHardRules is rewritten around a quote-aware scanner
that splits a line into operator-separated segments and only
auto-allows when every segment is provably read-only and
there is no substitution, expansion, or redirection. This
closes holes where a read-only first token (ls, cat) chained
to a mutator (&&, ;, |, redirection) auto-allowed. The git
allowlist is restricted to subcommands that are read-only
regardless of arguments (config/branch/tag/remote now need
approval), and the find mutation check covers
-fprint/-fprintf/-fls in addition to -delete/-exec/-ok.

CommandSafetyChecker now delegates to AutoModeClassifier:
the deterministic hard rules run first, falling through to a
one-shot LLM side-query against the user's configured
conversation model. Only an unambiguous allow is treated as
safe; errors are fail-closed. The check is available
whenever AI is configured rather than gated on a specific
vendor or OS version.

Add an on-device Apple Intelligence backend
(.appleIntelligence API, apple vendor) wired through a
FoundationModels bypass in AITermController for future use
as a dedicated classifier model. It is hidden from the model
picker.

2026-06-03: Add AutoMode classifier for AI command safety

Introduces an LLM-backed classifier that decides whether an
AI-proposed action may run automatically, needs manual
approval, or is blocked.

- TerminalHardRules: deterministic allow/deny rules applied
before the LLM.  - AutoModeClassifier: scopes the
classifier's view of the conversation to
  user text and proposed tool calls only (assistant text is
  intentionally unrepresentable, since the main model is
  untrusted from the classifier's perspective) and runs a
  one-shot side-query to reach a verdict.
- AutoModeTranscript: projects LLM.Message history into the
classifier's
  restricted TranscriptEntry form.

Covered by AutoModeClassifierTests, AutoModeTranscriptTests,
and TerminalHardRulesTests (44 tests).

2026-06-03: apply_layout: support inline new_session leaves

Let App.async_apply_layout create brand-new sessions in
place via new_session leaves, instead of only rearranging
existing ones. A new session is minted tab-less and spliced
into the destination tab through the same detach/adopt
machinery used for cross-tab moves, so it is born in its
final position and sizes/inherits cwd like an interactive
split.

- Resolver validates every new_session profile GUID up front
so a bad
  profile fails side-effect-free; replaces the blanket
  newSessionLeafNotSupported rejection with unknownProfile.
- Mutator createNewSession mirrors the makeWorkgroupPeer
path and honors
  the profile's working-directory mode
  (Home/Custom/Reuse-previous) by forcing the computed
  directory past the canPrompt:false short-circuit.
- Sessions minted during a transaction but never adopted
(e.g. a later
  attach throws) are terminated in endTransaction so a
  failed plan does not leak a headless shell. Scoped to
  created sessions only, never to detached user sessions
  from an aborted cross-tab move.
- Bump API protocol version to 1.16 and gate the feature
behind
  capabilities.supports_apply_layout_new_session; iterm2
  module 2.20.

Creating whole new tabs/windows remains unsupported.

2026-06-03: Fix unbalanced brace that broke the ModernTests
build

testRightPromptBug (added in 131b9c609) was inserted without
closing the preceding test function, so it nested inside it
and left the LineBufferTests class one brace short. The
whole ModernTests target failed to compile. Add the missing
brace; testRightPromptBug now runs as its own test.

2026-06-03: API: skip __pycache__ in AutoLaunch; sort
context menu items by name

Two Python API ergonomics fixes:

- AutoLaunch no longer warns that a __pycache__ directory is
a
  malformed script. Python drops one next to single-file
  scripts, so it is now skipped silently like dotfiles.

- Context menu items registered via the Python API were
displayed in
  the underlying dictionary's hash order, which is
  unpredictable and shuffles between launches. They are now
  sorted by display name, giving a stable order that a
  script can control by prefixing titles.

2026-06-02: Restore OSC 133 k=/aid= shell integration
submodule pointer

Point iTerm2-shell-integration at dd49a35f (emit OSC 133
aid= on A/B/C/D, plus k=s around PS2), the commits the
terminal-side OSC 133 support in 131b9c609 depends on. These
were dropped when 3.7.0beta3 reverted the pointer to
origin/main to get CI green.

2026-06-02: 3.7.0beta3

2026-06-02: AI chat: screen fidelity, TUI submit
reliability, bubble polish

Round of fixes after a wire-log review of a confused
orchestration session:

* get_screen_contents now returns real Unicode. The old
  WorkgroupIntrospection path called
  VT100Grid.compactLineDump / compactLineDumpWithHistory,
  which is a DEBUG dump that substitutes '.' for empty
  cells, '?' for any character above 127, 'U' for complex
  chars, '-' for double-wide right halves, etc. For an Ink
  TUI like Claude Code (mostly Unicode box-drawing) the
  agent saw gibberish and had no idea what was on screen.
  Replaced with iTermTextExtractor.content(in:...), the same
  path PTYSession's RemoteCommand surface and ToolCodecierge
  use. Trailing whitespace per line is stripped by the
  extractor; trailing blank rows are stripped by
  stripTrailingBlankLines. Image cells (which the extractor
  would otherwise drop) become a [image] placeholder via the
  attributeProvider + collapseImageAttachments pass  one
  marker per distinct NSImage instance, so a 20-cell-wide
  image emits one [image] not twenty.

* send_text reliably submits on Ink TUIs. typeIntoPTY now
always
  wraps the body in BPM markers when bracketed-paste mode is
  on (single- or multi-line), then waits 75ms via Task.sleep
  before writing the bare \r. Background: for short payloads
  two separate writeTaskNoBroadcast calls produce two PTY
  events on the slave side, which Ink treats as
  paste-then-submit. For ~500-byte payloads the kernel can
  chunk the master-side write across multiple slave reads,
  and an immediately-following \r gets absorbed into the
  same input burst Ink heuristically treats as paste
  content. Always-wrapping gives Ink an unambiguous ESC[201~
  end-of-paste; the 75ms delay covers the case where Ink is
  still ingesting when our \r lands. doSendText awaits
  typeIntoPTY so the tool_result holds back until \r has
  been transmitted, and disable_parallel_tool_use keeps the
  next tool call from racing. ESC/Ctrl-C and other control
  bytes still skip the wrap (those need to be interpreted by
  the TUI, not pasted).

* Chat bubbles no longer render Anthropic's leading "\n\n".
Anthropic
  models commonly prefix tool-using replies with two
  newlines; we persist that verbatim in the data model
  (round-trip parity with what the model emitted) but the
  rendition layer now trims leading whitespace before
  building the attributed string. Affects .plainText,
  .markdown, and .explanationResponse arms equally.

Wording cleanup, while we're in here:

* Approval bubbles re-phrased.
  - Spawn approval: "Approve opening 'New session'?"
  (placeholder
    name in quotes, reads as if it were a proper noun) is
    now "Open a new session?". Specifics still in the body.
  - Workgroup/session claim: "Approve writing to workgroup
  'X'?" (the
    word "writing" reads like file I/O) is now "Allow agent
    to control workgroup 'X'?". Body text also updated:
    "future write actions on this workgroup" -> "future
    control of this workgroup".

* Renamed CockpitChat directory and file. The orchestration
mode and
  the (unrelated) Cockpit feature used to share
  infrastructure; they don't anymore. git mv
  sources/ClaudeCode/CockpitChat -> Orchestration and
  CockpitChat.swift -> WorkgroupWatcher.swift (matches its
  only struct). pbxproj group path, file ref path,
  build-file ref, and parent group's child entry all
  retargeted.

* System prompt de-Claude-Code-ified. Opening no longer says
"you
  drive Claude Code sessions and other terminal sessions"
  just "terminal sessions". The "Chat role is the user's
  Claude Code CLI session" warning generalized to "the user
  is conversing with the program directly". "Newly-spawned
  programs" example list no longer leads with "Claude Code's
  first launch"; vim/less/htop/emacs/fzf cover it.
  "Ink-based TUIs like Claude Code" -> "most modern
  interactive TUIs". The Claude-Code-specific slash-command
  warning generalized to "don't invent or guess slash
  commands for the program running in a role". The
  orchestrator's tool surface is meaningfully
  programmer-agnostic; the prompt should be too.

* In-tree comments and DLog tags updated to match
(orchestrator
  instead of cockpit; Orchestrator dispatcher: instead of
  Cockpit dispatcher:).

2026-06-02: OSC 133 k= and aid= support

Implement OSC 133 k= (Semantic Prompt prompt kind: i/s/c/r)
and aid= (nested shell-integration session id) attribute
support end-to-end.

Parser (VT100Terminal) handles key=value arguments on
A/B/C/D/P.  Marks gain kind, excludedSubranges,
firstLineOfCommand/fullCommand, and
aid/parentAid/ancestorAids. Non-initial prompts (k=s/c/r)
add excluded subranges to the active mark without breaking
the prompt state machine or paste-each-line gate. aid=
populates a registry so D;aid=X targets the matching mark
and an outer-D cascade-closes inner marks (e.g. ssh tunnel
drops where the inner D never arrives).

Mark coord storage (promptRange / commandRange / outputStart
and the excludedSubranges endpoints) moves to
ResilientCoordinate so it survives folds, resizes, and
scrollback overflow. Saved-tree (alt- screen) marks observe
a separate RC pool whose resize broadcast is built against
altScreenLineBuffer rather than the primary linebuffer.

Consumers (Select Current Command, share URL, AI explain,
the Python GetPromptResponse.excluded_subranges field, etc.)
subtract excluded subranges from the typed-command range.

Shell integration scripts (bash, zsh, fish, tcsh, xonsh)
emit k=s around PS2 and aid= on A/B/C/D. New /v2/ scripts
ship alongside legacy /v1/.

Fixes issue 5749 (Advanced Paste hangs on PS2).

2026-06-02: Bundled protobuf runtime: 3.19.4 to 35.0 +
protoc 3.21 toolchain

Vendor v35.0 of the ObjC protobuf runtime under
ThirdParty/ProtobufRuntime and split tools/build_proto.sh
into two protoc invocations: latest protoc for ObjC
(GOOGLE_PROTOBUF_OBJC_VERSION >= 30005 required by the new
runtime), and protoc 21 / 3.21.x for Python (protoc >= 22
emits gencode that drops Python 3.7+ support, which the
iTerm2 Python SDK still promises). Both passes overridable
via PROTOC_OBJC / PROTOC_PYTHON.

Regenerated sources/proto/Api.pbobjc.{h,m} and
api_pb2.{py,pyi} against the new toolchain.

2026-06-02: Browser: cloak JS surface in captcha frames; fix
geolocation permissions

Cloak: - In known captcha/challenge frames (Cloudflare
Turnstile, hCaptcha,
  Arkose/FunCaptcha, reCAPTCHA) strip window.webkit and skip
  the page-world bridges (console wrap,
  Notification/geolocation polyfills, audio mute/monitor) so
  the probe sees a stock Safari surface.
- Detection lives in one shared snippet
(challenge-frame-detection.js)
  that the cloak and each bridge recompute independently via
  the template loader's INCLUDE directive. No state is
  passed through a window property: a marker like
  window.__iTermBrowserCloak would itself be a detectable
  fingerprint (real Safari has no such property).
- Match against the anchored pathname and actual query keys,
not a loose
  substring of the full href, so a page cannot trip the
  cloak by putting a marker in its query value or fragment.
- Do not install a stand-in if delete window.webkit ever
fails; an own
  "webkit" property whose value is undefined is a worse
  fingerprint than the original.

Geolocation/notification permissions: - Stop persisting
system-level denials (Location Services off, OS prompt
  declined, notifications disabled at the OS). Only an
  explicit Allow or Block in the per-site dialog is
  remembered, so re-enabling the system permission lets the
  site prompt again instead of staying blocked.
- Geolocation denials now send a PERMISSION_DENIED error to
the page
  instead of being silently dropped, so errorCallback fires
  and the page does not hang.
- Route success/error callbacks and the pushed permission
state to the
  originating frame instead of always the main frame.
- clearWatch and cancelOperation bypass the permission gate;
they only
  stop in-flight work, so they must run regardless of
  permission state.

2026-06-02: Fix heavy window border on transparent windows
and stale border frame

Two related fixes for the heavy window border (issue 12882):

- Window corner-radius detection failed on low-opacity
windows
  because it required near-opaque content (a fixed alpha
  threshold of 180). The window mask is encoded in the alpha
  channel at any opacity, so threshold each row relative to
  its peak alpha instead.  Add a per-OS fallback radius
  (macOS 26 uses rounder corners) for when detection has not
  completed or fails.

- The iTermWindowBorderView frame was not kept in sync with
the root
  view: it could be left at a stale size from an earlier
  window size, stroking the border as a smaller inner
  rounded rectangle. Set its frame to the root bounds in
  layoutSubviews and updateBorderViews, matching how the
  other subviews are laid out.

Adds unit tests for opacity-independent corner-radius
detection and the fallback radius.

2026-06-02: Narrow announcements when clippings panel is
visible

The announcement frame used the full SessionView width,
ignoring actualPanelReservation (the width reserved for
visible right-gutter panels like clippings), so
announcements overlapped the clippings panel. Subtract the
reservation in updateAnnouncementFrame and
showNextAnnouncement, matching the existing diff/code-review
overlay behavior. Toggling clippings recomputes the width
via updateLayout.

2026-06-01: AI live harness: positively verify Gemini audio
+ video (inlineData)

Gemini's inlineData audio/video paths had no positive live
coverage: the matrix fixtures (synth mp3, scroll-browser
mp4) lack a deterministic probe so those cells were skipped.
Generalize the audio-probe helper to a media-probe helper
and add a deterministic probe video (pinecone shown on a
frame and spoken in the audio track), then add: -
test_gemini_audioTranscribe_{mp3,wav} -
test_gemini_videoDescribe

All return the probe word, confirming Gemini inlineData
round-trips for audio (mp3/wav) and video end-to-end. The
matrix skip comments now point at these tests instead of
reading as an open gap.

2026-06-01: AI live harness: positively verify OpenAI
input_audio (mp3 + wav)

The attachment matrix only exercised input_audio against
gpt-4o-mini, which rejects the content type at the schema
layer (it isn't an audio model) - so it proved nothing about
whether a real audio model accepts our shape. Add a
deterministic spoken-probe fixture (a short TTS clip saying
the magic word) in wav and mp3, and a live audioTranscribe
test driving a synthesized gpt-audio chat-completions model
for both formats.  Both round-trip and the model returns the
probe word, confirming the input_audio serialization
end-to-end. Also pin the wav format mapping in an offline
unit test (mp3 was already covered).

2026-06-01: AI: gate attachments by modality and wire OpenAI
image/audio input

Attachment capability is now decided per modality and
serializer rather than a per-format allowlist or model-name
match. accepts() allows any file a vendor's request format
can carry as a typed content block (text, image, PDF, audio,
video) and relies on the backend to reject an unsupported
sub-format or a non-capable model with a clean error that
surfaces in the chat. This stops predicting which specific
model supports vision/audio, so a newer or custom model on a
known host works automatically.

Serializer changes: - OpenAI chat-completions
(CompletionsMessage): emit image_url for
  image/*, input_audio (wav/mp3) for audio; SVG and other
  text stay text.
- OpenAI Responses: emit input_image for image/* instead of
input_file
  (which 400s image MIMEs); images bypass vector-store
  upload so they reach the inline vision path even when a
  store is configured.
- Anthropic: send any image/* as an image block with its
real media
  type and let Anthropic reject formats it doesn't accept.

Adds serializer unit tests for the OpenAI image/audio
shapes, an OpenAI imageDescribe live test, and re-baselines
the live attachment matrix: OpenAI png/webp now
accept-with-vision; heic/tiff and non-audio-model audio
reject at HTTP; Anthropic heic/tiff reject at HTTP. Verified
with the live harness (attachment matrix, imageDescribe,
smoke, toolCall, multiTurn) across OpenAI, Anthropic,
Gemini, and DeepSeek.

2026-06-01: AI chat: offer to attach dropped files instead
of inserting path

Dropping a file onto the chat text field used to insert its
path as text (plain NSTextView behavior). When the current
backend accepts the file type as an attachment, prompt to
attach it instead. The choice is rememberable via a
suppressible warning. Unsupported types keep the legacy
path-insertion behavior.

2026-06-01: Add BairesDev sponsor image

2026-06-01: Add sponsor

2026-06-01: Scroll code review session to bottom on prompt
submit

When submitting a code review prompt, the overlay panel is
removed but the session was not always scrolled to the
bottom. Snap to the end so the launched program's output is
visible and the session follows new output.

2026-06-01: Expand {DETAIL} guidance in code review system
prompt

Instruct the reviewer to frame each finding as a fix-it
prompt for an agent that wrote the buggy code: lead with a
succinct problem statement and focus on what is wrong, why,
and how to fix it.

2026-06-01: Make the Code Review system prompt editable in
Settings

The Code Review system prompt was hard-wired to the bundled
code-review-system-prompt.txt. It is now editable in
Settings > General > AI > Prompts, with the bundled file as
its registered default.

At launch, wrappedCommandForCodeReview writes the configured
value to a per-session temp file and binds its path to a new
codeReviewSystemPromptFile variable; the review peer's
command template feeds that to claude
--append-system-prompt-file. A one-time migration rewrites
persisted Code Review commands (both the templated
appBundlePath form and older resolved-absolute-path forms)
onto the new variable, leaving the rest of the command and
any user customizations untouched.

Also fix resetAIPrompt: to reset the editor to the selected
prompt's actual default instead of always showing the Engage
AI default.

2026-06-01: AI: Anthropic prompt caching on system + tools

Mark the system text block and the last tool definition with
cache_control: ephemeral so Anthropic's prompt cache fires
on the stable prefix of every chat request. The
orchestration chat carries ~8KB of system prompt and ~5KB of
tool definitions on every turn; with caching enabled the
second and later requests within the 5-minute window pay
0.1x the input rate for that ~13KB segment instead of full
price.

Two breakpoints, not one, by intent. Anthropic's prefix
order is tools to system to messages, so:

  - Marker on the last tool covers [tools] only. Fallback
  segment
    that still hits when the system prompt changes (a future
    per-turn system addendum, for example).
  - Marker on system covers [tools + system]. Common-case
  longest
    match: both stable for the chat, the whole prefix
    becomes one cache_read.

Dropping the last-tool marker as "redundant" because system
already covers tools would silently destroy the fallback
segment. Doc comment in body() spells this out.

Body now sorts JSON object keys via
JSONEncoder.outputFormatting =
.sortedKeys. JSONSchema carries a [String: Property] dictionary;
Swift's Dictionary iteration order is randomized per
process, so without sortedKeys two requests built from the
same tool list could serialize their tools array with the
same bytes shuffled, making cache_read silently never fire.
The byte-determinism test caught this.

system field changed from String? to
[AnthropicSystemBlock]?. The array form is the structured
shape Anthropic accepts for cache_control markers.
Wire-compatible: Anthropic accepts either form. Requests
without a system message omit the field entirely (renames,
no regression).

Tests:

  - ModernTests/AnthropicPromptCachingTests.swift: 13
  offline tests
    pinning the wire shape (cache_control on the right
    blocks, not on the others, omitted when system/tools are
    absent) AND the byte- determinism invariant (identical
    inputs produce identical bodies; same system+tools
    across different messages produce byte-identical system
    and tools segments).
  -
  AILiveHarness.test_anthropic_promptCaching_cacheReadAfterCacheCreation:
    end-to-end against the real server. Issues two
    back-to-back requests with the same system prompt and
    different last user message, asserts call 1 reports
    cache_creation_input_tokens > 0 and call 2 reports
    cache_read_input_tokens > 0. Padding pushes the system
    prompt above Haiku's 2048-token minimum cacheable size
    (Sonnet/Opus are 1024); below that threshold Anthropic
    silently ignores the marker.

AILiveHarness side fixes (pre-existing test-target
compilation breakage that was blocking all tests, not just
this round):

  - AILiveDriver.AILiveRunResult gains
  capturedResponseBodies so live
    tests can parse server-emitted usage fields.
  - RemoteCommand passed to .remoteCommandRequest(...)
  updated to wrap
    in .classic(...) (RemoteCommandPayload refactor that
    landed on master).
  - AILiveHarness class marked @MainActor so its calls into
    ChatAgent.aiMessagesForReloadingTranscript (now
    main-actor-isolated) compile.

2026-06-01: Chat orchestration: fix start_session paths and
prompt clarity

Round of fixes to start_session and related code paths
surfaced by the cockpit wire log:

* start_session command runs through the user's login shell.
When the
  agent supplies a command (e.g. "claude"), the dispatcher
  now sets KEY_RUN_COMMAND_IN_LOGIN_SHELL on the launch
  bookmark, so the command runs via /usr/bin/login +
  ShellLauncher and inherits the user's interactive PATH and
  dotfiles. Without this, the bundled app's PATH is missing
  Homebrew, ~/.local/bin, ~/.npm-global/bin, etc., and a
  bare command like "claude" hits command-not-found, the
  shell exits, the only session in the new window
  terminates, the window auto-closes, and the LLM gets a
  workgroup_id pointing at nothing.

* start_session reports failure synchronously (no sleep)
when the
  spawned session is already gone by the time
  launchBookmark's "ok" fires. session.exited ||
  !iTermController.anySession(withGUID:) catches the
  same-runloop collapse. The agent now gets a clean
  unsupported error instead of a dangling workgroup_id.

* start_session with window="current" now actually splits.
The
  dispatcher resolves keyTerminalWindow / currentTerminal
  and passes it as inTerminal: so launchBookmark takes the
  verticalSplit branch instead of fabricating a fresh
  zero-tab window and falling through to a new-tab path.
  Errors clearly when no terminal is open.

* iTermSessionLauncher's split path honors the launcher's
command
  override the same way the tab path does  mutates
  KEY_CUSTOM_COMMAND / KEY_COMMAND_LINE on the profile when
  _command is set so split panes actually run the requested
  program instead of the profile's default shell.

* WorkgroupIntrospection.resolve no longer conflates
"synthetic
  workgroup whose session is gone" with "real workgroup that
  doesn't have that role." Added resolveTargetOrThrow that
  maps the former to targetNoLongerExists and routes
  doGetState, doGetScreenContents, doRegisterWatch,
  doSendText, doInterrupt, doStartCodeReview through it.

* New WorkgroupIntrospection.spawnWorkgroupID sentinel
("spawn") so
  the dispatcher's promptForSpawn and the chat view's bubble
  renderer no longer duplicate the literal across files. The
  bubble shows "Approve opening 'New session'" for spawn
  requests, "Approve writing to session " for synthetic
  workgroups, and "Approve writing to workgroup " for real
  workgroups.

* OrchestrationToolProvider's activity-line renderer no
longer prints
  "_unknown role_" when the model hallucinates target as a
  string (the recurring "\n  <workgroup_id></workgroup_id>\n
  <role></role>\n" shape). It extracts the inner tags and
  renders them; falls back to "_(malformed target)_" only
  when nothing decodable is present. The dispatcher still
  rejects the call as malformed_args.

* OrchestratorTarget doc expanded to spell out the two
shapes
  workgroup_id can take (real instance ID, synthetic
  session: prefix).

* System prompt instructs the agent to call
get_screen_contents
  before typing into a session it hasn't already verified
  the state of  newly-spawned programs (Claude Code's first
  launch, vim, less, fzf, login wrappers, anything with a
  confirmation overlay) often open at a prompt the agent
  hasn't seen, and typing the intended payload into that
  prompt corrupts the session.

* ChatAgent.message() no longer it_fatalError's on
.watcherEvent.
  Watcher fires are user-author messages and the agent's
  reply renders as plain markdown / multipart like any other
  turn. Same fix applied to the attachment-streaming
  sibling.

* OrchestratorCommand.allToolDefinitions /
OrchestratorDispatcher /
  related doc comments updated to "spawn" sentinel instead
  of the stale "new-session" text and to reflect the
  renderer's actual key.

2026-06-01: Add disk wire logger for raw AI API traffic

New advanced setting aiChatRawWireLogging captures every
byte sent to and received from the AI vendor: full request
headers, request body, streamed SSE chunks as they arrive,
the final response or error, and elapsed time. Writes to
~/Library/Application
Support/iTerm2/AIChatWire/aichat-wire-<stamp>- pid<N>.log,
one file per app launch.

Hooked in at iTermAIClient.request, below the JS plugin
boundary, so the bytes logged are the bytes the plugin is
about to ship (or just received), not a post-parse view that
might mask the bug being debugged. A per-call UUID stamps
every record so concurrent calls are untangled in the log.
The existing test-harness liveObserver and the new logger
coexist; both observe the same wrapped stream.

Records are written on a serial queue with one shared file
handle opened lazily on first use; off-path cost is one
isEnabled check.

WARNING (documented in the setting description): the log
captures Authorization / X-Api-Key headers and prompt +
response content verbatim. The whole point is full fidelity
for debugging, so no redaction at the write layer. The
companion parser script redacts auth headers only at display
time.

Companion: tools/parse_ai_wire_log.py reconstructs streamed
Anthropic responses back into the content blocks the
non-streaming API would have returned in one shot
(text_delta fragments glued together, input_json_delta
fragments concatenated and JSON-decoded per tool_use block),
and renders the request side (model, system, tools, message
thread with tool_use / tool_result blocks broken out).
Supports --call <UUID> filtering, --raw passthrough, and
--max-body truncation.

2026-06-01: Chat orchestration: fix lifecycle, race, and
validation issues

Address a batch of code-review items on the chat
orchestration path:

* OrchestratorDispatcher.deinit's beta/nightly tripwire
asserted on
  state that was never drained before deinit
  (brokerSubscription is set once in init and nilled by the
  deinit cleanup itself, so the "not torn down" assert was
  structurally always-true). Replaced with a
  Thread.isMainThread check that actually catches the
  off-main deinit case it was supposed to flag.
* ensureClaim() raced on concurrent writes against the same
unclaimed
  workgroup: both calls passed the synchronous contains
  check, both spawned a permission prompt. Added
  pendingClaimTasks so later callers await the in-flight
  Task instead of stacking bubbles.
* dropDispatcher only removed the dict entry; an in-flight
Task with a
  strong ref to the dispatcher would keep delivering
  tab-status notifications, resume parked prompts as if
  live, and publish a posthumous tool_result into a chat
  that had left orchestration mode.  Added a tornDown flag
  and tearDown() that synchronously unsubscribes, removes
  observers, cancels pending claim tasks, and resumes parked
  prompts with false. Every entrypoint (handleToolCall,
  broker handler, tab-status, session-terminate) now
  early-returns on the flag.
* handleSessionToolCall had no once-guard or timeout on the
  session.execute continuation; a dropped completion
  (browser command whose WebView is gone, SSH lookup that
  never replies) parked the LLM turn forever. Wrapped with
  withResolvedOnce(timeoutSeconds: 30) like doStartSession.
* sessionStateHistory grew unbounded: the tab-status
observer wrote
  for every session app-wide, not just sessions we watched.
  Gated on watchers.contains so the dict only ever grows for
  watched GUIDs.
* doAddWorkgroupClipping now validates args.type (non-empty,
<=80
  chars) and routes through PTYSession.addClipping so the
  read-modify- write lives in one place.
* notify_user was advertised, decoded, and dispatched but
the
  implementation just threw notImplemented. Removed the
  ToolName case, decode arm, dispatch arm, doNotifyUser
  stub, NotifyUserArgs, and the userFacing Category.
* Chat.encodeIDList claimed an it_assert for the
newline-free
  invariant that wasn't actually in the code. Added it.
* CockpitWindowController's search field now actually
filters: a post-
  rebuild prune keeps session leaves whose title matches and
  removes parents that become empty. Identity-stable so
  expansion and selection survive filter edits.
* LLMProvider.accepts short-circuited image/audio/video
MIMEs at the
  per-vendor allowlist before reaching shouldUploadFile,
  silently rejecting attachments
  OpenAI/responses+vectorStore can ingest via upload. Falls
  through to shouldUploadFile now.
* Added the messageID == originalMessageID staleness guard
to the
  workgroupPermissionRequest and enableOrchestrationRequest
  cell button handlers, matching the sibling cases.
* Updated stale doc-comment counts (28 tool cases -> 24, 16
  orchestrator tools -> 12) and the JSONSchema empty-array
  note.

2026-06-01: Add orchestration mode in chat.

2026-06-01: Treat any two localhosts as the same host on
host change

The host-change heuristics in screenCurrentHostDidChange
compared hostnames as bare strings, so a local .local name
drift (e.g. mDNS renaming MacBook-Pro-2.local to
MacBook-Pro-3.local) or a sudo/su could be seen as a real
host change. That spuriously offered to reset terminal state
and to restore the tab and window title.

Short-circuit the heuristics when both the previous and new
hosts have a frozen locality of localhost, regardless of
name or username.

2026-06-01: Badge: keep clear of the right-gutter clippings
panel

Right-align the badge at the inner edge of the right-gutter
panel area by subtracting panelReservation from the
available width, in both the legacy and GPU renderers.
Previously it used the full PTYTextView width and drew
behind the clippings panel.

2026-05-29: Reject notify-on-termination for already-dead
PIDs

beginMonitoring now checks with kill(pid, 0) whether the
process still exists. If it is already gone (ESRCH), arming
a DISPATCH_PROC source would never fire, so show an
explanatory alert and return false instead of failing
silently. EPERM (process owned by another user) still
proceeds because NOTE_EXIT does not require signal
permission.

2026-05-29: Fix stacked image views on recycled job cells

iTermJobTreeImageTableCellView allocated a fresh NSImageView
and added it as a subview on every call, including cell
reuse, leaving the previous image views as orphaned
subviews. Reuse the existing image view when the cell is
recycled and only create one the first time.

2026-05-29: Add "Notify on Termination" for jobs

Right-clicking a job in the jobs toolbelt or the status bar
job popover now offers "Notify on Termination." Monitored
jobs show a bell next to their name, and when the process
exits a modal alert names the job and process ID.
Simultaneous exits coalesce into one alert.

The bell is an SF Symbol attachment tagged with
iTermDynamicProfileSymbolName and retinted via a shared
NSTextField helper so it follows selection and light/dark
appearance, the same dance the profiles list uses.

2026-05-29: Clippings: prefix each with bold title and add a
configurable separator

Sending or copying multiple clippings previously joined them
with a bare newline, so they ran together. Each clipping is
now prefixed with its title in bold Markdown, and multiple
clippings are joined with a new Clipping Separator advanced
setting (default is a line of two dashes, using Vim escape
syntax).

2026-05-28: Jobs view: add table outline and tighten
toolbelt layout

Give the jobs outline view a line border so it matches the
other toolbelt tables, which all get one from
scrollViewWithTableViewForToolbelt.

Fix the bottom toolbar layout. sizeOutlineViewToFit was
insetting the scroll view by NSMaxY(kill_.frame), but kill_
lives inside _signalContainer so that value (21) was in the
wrong coordinate space.  Replace it with a configurable
toolbarHeight, clamp the scroll view height so degenerate
startup sizes can't produce a negative-height frame, and
anchor the controls with a configurable controlsBottomMargin
instead of centering them against a possibly-stale scroll
view frame.

Add useVisualEffectView so hosts that already supply a
background can suppress the VEV. The toolbelt now drops the
VEV, uses a 28pt strip with the controls flush to the bottom
(6pt above, 0 below), while the status bar popover keeps the
VEV/glass and its default 38pt centered strip.

Set useGlassEffectView before -font in the popover so
awakeFromNib sees it when the view loads.

2026-05-28: Session Stats: show custom peer-switch shortcuts

peerSwitchShortcutLabel always rendered the built-in
<position> shortcut and ignored a peer's configured
peerSwitchShortcut. Add customShortcutLabel(forPeerID:) on
iTermWorkgroupPeerPort and consult it first, falling back to
the built-in shortcut only when no custom one is set,
matching WorkgroupModeSwitcherItem's precedence.

2026-05-28: AI chat: repair orphan tool_result on chat
reload

An auto-approved ("always") remote command historically
squelched the remoteCommandRequest from the persisted
transcript while keeping the remoteCommandResponse. On
reload, ChatAgent.load(messages:) rebuilt the prompt from
that transcript and the result was an orphan tool_result
with no matching tool_use, which Anthropic rejects with 400
'unexpected tool_use_id found in tool_result blocks'. The
bug predates v3.6.10, so shipping conversations on disk
already contain orphans.

AIChatToolCallRepair walks the rebuilt prompt and, when it
finds an orphaned functionOutput, inserts a synthesized
assistant functionCall immediately before it. Two pairing
modes have to be satisfied because they reflect what the
vendor serializers actually do:

  * Id-based (Anthropic, OpenAI Responses): match on call
  id. The inner
    FunctionCall.id MUST equal the wrapper
    FunctionCallID.callID; CompletionsAnthropic keys
    tool_use blocks off the inner id and ignores the
    wrapper, so a nil inner id would serialize the synth
    call as plain text and reproduce the 400.

  * Position-based (Gemini, legacy OpenAI function_call):
  the
    deserializer sets both the inner FunctionCall.id and the
    wrapper FunctionCallID to nil (Gemini.swift parser).
    Those nils flow into the persisted
    .remoteCommandResponse and arrive here as a
    functionOutput with no id at all. Such a result cannot
    be paired by id, so synthesize a nil-id functionCall and
    rely on adjacency. The previous nil-id drop was silently
    dropping every Gemini tool result on reload, leaving an
    orphan functionCall instead of an orphan
    functionResponse.

Also fixes a second-order bug in role(from:) that the new
live coverage caught: tool results were being given
LLM.Role.user (because the chat Message author is .user),
but DeepSeek's serializer keeps the wire role as 'user' (and
rejects the request as 'insufficient tool messages') while
OpenAI Responses logs 'Unexpected user message body' and
silently drops the item. Production live calls already
construct tool results with role=.function (AITerm.swift
around line 957); this brings the reload path in line.
Anthropic and Gemini accept either.

Tests =====

Offline (ModernTests/AIChatToolCallRepairTests):
  * Id-based: orphan synthesizes preceding tool_use with
  matching inner
    id; well-formed pair untouched; multiple orphans
    repaired independently; reported scenario from the bug
    report.
  * Position-based: nil-id pair untouched; nil-id orphan
  synthesizes
    adjacent nil-id tool_use; multiple nil-id orphans
    repaired.
  * Wire format:
  testOrphanRepair_serializesAsAnthropicToolUse builds
    the Anthropic body and asserts the tool_use block lands
    before the tool_result with matching id (closes the gap
    that let the inner-id bug ship);
    testOrphanRepair_serializesAsGeminiFunctionCall does the
    same for the Gemini functionCall/functionResponse Part
    adjacency.

Live (AILiveHarness):
  * ChatAgent.aiMessagesForReloadingTranscript seam exists
  so the live
    harness can reach AIChatToolCallRepair through the same
    pipeline ChatAgent.load(messages:) uses without
    instantiating ChatBroker (which requires
    ChatDatabase.instance and would write the user's real
    chat DB during tests).
  * runChatReload drives a synthetic [Message] transcript
  through the
    seam, appends a new user message, and asserts the
    round-trip completes. For the Anthropic orphan case it
    inspects the captured request body and asserts a
    tool_use block with the matching id precedes the
    tool_result.
  * 20 per-vendor wrappers: anthropic, gemini, openai
  (Responses),
    openai (chat-completions synthetic), deepseek, each in
    {paired, orphan} x {streaming, non-streaming}.
  * AILiveHarness.loadConfig() caches the first successful
  read so
    Xcode test-environment NSTemporaryDirectory rotation
    between -only-testing methods does not spuriously
    XCTSkip the run.
  * tools/run_ai_live.sh registers the 20 new method names.

Verification ============

Full live sweep against real vendor APIs: 20/20 pass after
one retry of an o3-pro network timeout. Per vendor:

  anthropic                  4/4 gemini
  4/4 (the nil-id fix) openai (Responses)         4/4 openai
  (chat-completions)  4/4 deepseek                   4/4
  (the role(from:) fix)

Also exercised the offline suite (10 tests, including the
two wire- format tests) red->green to confirm both
wire-format tests catch the respective bugs when
reintroduced.

Harness housekeeping ====================

Added gemini-3-pro-preview to
AILiveHarness.unreachableForNewKeys.  Google fully retired
the model and returns HTTP 404 'no longer available' for all
keys, so the default Gemini sweep was failing every test on
that model. AIMetadataFixtureCoverageTest honors the same
set and now correctly skips its refusal-fixture requirement
for it.

2026-05-28: Add spreadsheet with shortcuts

2026-05-27: Script Console: preserve scroll/selection and
fix dark mode logs

The console used to call -reloadData on the whole script
list whenever an entry was added or a script stopped. That
reset the table's scroll position and, for a view-based
table with an unchanged selection index, dropped the visual
selection highlight even though the detail view kept showing
the selected entry.

Replace the blanket reloads with incremental row
insert/remove. Both the "All" and "Running" filtered lists
are order-preserving subsequences of the same entries array,
so a snapshot diff (_displayedEntries vs the live
filteredEntries) identifies exactly which rows to insert or
remove and leaves existing rows, their selection highlight,
and scroll position untouched. A consistency guard falls
back to a full reload if the baseline ever desyncs.

Because incremental removals now shift the selection to
follow its entry without firing a selection-changed
notification, the per-entry observer no longer trusts its
captured row index; it resolves the live row from the
observed entry and skips the reload if the entry is no
longer shown.

Also preserve the output pane's scroll position when a
filter rewrites its contents on each new line, consolidate
the Terminate/Start enable logic into -updateButtonsEnabled,
and set the Logs text view's color to the dynamic textColor
so it is readable in dark mode (the Calls view already did
this).

2026-05-27: Make T the Undo Close shortcut; move Show Tabs
to U

T now triggers Shell > Undo Close, and View > Show Tabs in
Fullscreen moves to U.

To ease the transition, the first time a user invokes the
new T while in full screen, a one-time alert explains the
change and offers to add a global key binding that maps T
back to Show Tabs in Fullscreen (global bindings are
consulted before menu key equivalents, so this restores the
old behavior).

Because the Undo Close menu item is disabled when nothing
can be restored, its key equivalent does not fire in that
case, so iTermApplication's key-down path detects the
keystroke directly to still show the notice. The detection
runs after binding resolution so an existing T binding is
always respected, and it ignores leader-flagged events since
a leader sequence is a distinct keystroke.

2026-05-27: Session notes keep their distance from the
top-right corner on resize

When a session resizes, the floating session note now shifts
by the bounds size delta so it preserves its distance from
the top-right corner instead of the bottom-left. The note is
still clamped to stay within the session bounds.

2026-05-27: Open Quickly MRU: make recency follow focus
order

Open Quickly ranked switch-to-session rows by a per-session
lastActivityOrdinal that was stamped from too many places:
the low-level -[PTYTab setActiveSession:] (which fires on
structural changes like a pane closing, splitting, or
restoring, even in background windows) and unconditionally
in -reallySetCurrentTerminal:. During a cross-window jump
the destination window's previously-active sibling was
briefly its current session, so it got stamped just below
the target and then outranked the genuine previous pane. The
result was that Cmd-Shift-O Enter could land on a pane the
user never visited. Issue 12799.

Define MRU strictly as focus order and stamp only when the
focused session actually changes. The new iTermFocusOrder
singleton owns the monotonic counter and, via
IdempotentOperationJoiner, coalesces stamp requests to the
next runloop turn and reads the settled focused session (the
front window's current session). Transient mid-jump states
are never observed, and structural active-session changes in
background windows no longer reorder the list. The three
focus-change triggers (window became current, tab selected,
a tab's active pane changed) now just call -setNeedsUpdate.
The pane-change trigger is no longer gated on
updateActivityCounter, so cmd-[/] focus moves also update
MRU, while the per-tab activity counter that drives cmd-[/]
toggling is unchanged.

Add debug logging in iTermFocusOrder (what was stamped or
why it was skipped) and in the Open Quickly model (the
excluded current session and each candidate's ordinal,
recency, and score) so the ranking can be diagnosed from a
captured debug log.

2026-05-26: Preserve screen content when a non-alt-screen
program clears on launch

Some full-screen programs that don't use the alternate
screen buffer (for example Claude Code) repaint by moving
the cursor to the top of the screen and drawing over
whatever was there, destroying content the user can still
see. When such a program moves the cursor above the start of
its command's output before writing any output of its own,
scroll the visible screen into history first so it isn't
lost, as though it had sent CSI 2J.

The trigger is in terminalMoveCursorToX:y: and is gated on:
shell integration (a valid startOfRunningCommandOutput), not
being in the alternate screen, and a new
appendedTextSinceCommandExecuted flag (reset at FTCS C, set
on any text append). The flag, rather than a grid scan,
correctly handles relaunches where the previous session left
content on screen below the new prompt: that content was
appended before this command's FTCS C, so it is still
preserved.

Behind the saveScrollbackWhenCursorMovesAbovePrompt advanced
setting (default on). Requires shell integration.

2026-05-26: Recompute tab status when sessions join or leave
a tab

The aggregated tab status is a cached copy of the winning
session's status, refreshed only when a resident session
changes its own status (sessionTabStatusDidChange:). A
session can enter or leave a tab while already carrying a
status without any such change, leaving the tab showing a
stale or missing status (e.g. a "waiting" indicator stuck on
a tab after the pane that set it was closed or moved out by
a workgroup teardown).

Recompute the aggregate at every membership change: -
numberOfSessionsDidChange (count changes: close, move
between tabs,
  restore, workgroup adopt via the split-tree rebuilder)
- both swapSession: methods (count-preserving swaps) -
replaceWithContentsOfTab: (absorbing another tab's sessions)

Instant-replay and filter doppelgangers are intentionally
excluded: they carry no status, and the live session's
status remains correct.

2026-05-26: Fix subtoken logging

2026-05-26: Add urlHandlerCommand advanced setting

When set, clicking a URL (OSC 8 hyperlinks and auto-detected
URLs) runs the configured interpolated command via /bin/sh
-c instead of handing the URL to the OS. The URL is exposed
as \(url) and a background-open flag (whether option was
held) as \(background), which is 1 or 0. The command runs as
a background task whose output goes to the Script Console,
with a notification on failure.

Empty by default, so URL opening is unchanged.
iTerm2-internal schemes (iterm2:), scheme-to-profile
bindings, and Semantic History file links are unaffected.

Implements the request in issue 12878.

2026-05-26: Freeze localhost detection instead of
recomputing by hostname

Localhost-ness was decided by comparing a session's recorded
hostname against the live [NSHost fullyQualifiedDomainName]
(gethostname). When the machine's hostname changes (for
example after a network change), recorded local hosts stop
matching and are treated as remote, which broke the git
status bar, the workgroup diff list, opening local files,
and upload/download behavior.

Record locality on VT100RemoteHost when a host is reported
(the moment the reporting shell's name and gethostname are
contemporaneous) and freeze it:

- Add VT100RemoteHostLocality (unknown/localhost/remote), a
3-arg
  initializer, serialization, and copy/doppelganger carry.
  isLocalhost returns the frozen value and only falls back
  to the name compare when unknown (legacy data).
- Stamp locality in -setHost:user:viaSSHIntegration: at
report time,
  carrying it forward for user-only re-reports.
- Publish a new "isLocalhost" session variable mirroring it.
- Read the frozen value (model, variable, or the new mapper)
at the
  localhost checks that previously compared names:
  iTermGitStringMaker, the hostname status bar abbreviation,
  and the composer and path-completion host construction.
- Preserve serialized locality across the conductor-unhook
restore
  rather than re-stamping the restored host remote.

Rename the setHost/screenCurrentHostDidChange ssh: argument
to viaSSHIntegration:, since it describes the cause of the
host change (the SSH/conductor layer, which manages terminal
state itself) rather than whether the host is remote, and
cross-reference the two terminal-state cleanup paths.

Add a fakeFullyQualifiedDomainName advanced setting to test
hostname changes without renaming the machine.

2026-05-26: Render floating session note above the toolbar

Both the session note view and the toolbar (which hosts the
peer mode switcher) are added via addSubviewBelowFindView:,
which never ordered them relative to each other, so their
z-order depended on insertion order and the toolbar could
end up on top. Position the note above the toolbar
regardless of which is added first.

2026-05-26: tools/adhoc.sh: abort on build failure instead
of uploading an empty zip

2026-05-26: Avoid false-alarm state restoration timeout
right after login

The "Continue restoring state?" prompt fires when the SQLite
integrity check exceeds its timeout. At login the system is
busy restoring every app's windows and disk I/O is
saturated, so even a healthy database can exceed the 10s
clean-shutdown timeout. Extend the timeout to 120s during
the first ten minutes after boot. Also soften the prompt
wording and make Keep Waiting the default action instead of
discarding windows. Issue 11809.

2026-05-26: LineBuffer: rewrite blockContainingPosition, fix
multi-block lookups

fast_ and slow_firstBlockContainingPosition: now share
closed-right raw-position-only semantics: a position equal
to the end of block i is reported as "inside block i, at
offset rawSpaceUsed", and yOffset is the caller's concern.
This removes the May 2025 additionalRemainder ruler-
shopping hack and the April 2022 always-advance branch that
the hack was papering over; both lookup variants are now
simple and agree by construction.

Renamed to firstBlockContainingPosition: to be honest about
ambiguity. A position at a boundary, or any position when
intermediate blocks have zero rawSpaceUsed, is shared by
multiple blocks. The function returns the first such block;
callers that need the visually-correct block (only
positionForStartOfLastLineBeforePosition:width: today)
consume yOffset themselves via a forward walk in
_findPosition:'s new width variant.  AsyncFilter callsites
pass width through. Search keeps the width-less variant (the
raw-character starting position is correct regardless of
yOffset).

Also fixes coordinateForPosition:'s extendsToEndOfLine
handling: the non-lastPosition branch was clobbering x to 0
when extendsToEndOfLine was YES and extendsRight was NO,
inconsistent with the lastPosition branch. The structure now
mirrors the lastPosition branch so past-EOL coords
round-trip naturally.

Deleted dead code: setStartCoord:ofFindContext:width:,
storeLocationOfAbsPos:inContext:, absBlockNumberOfAbsPos:
(the third had only the second as a caller).

Added comprehensive unit and integration tests in
LineBufferTests covering fast/slow agreement, the 3-block
boundary case that triggered the investigation,
position/coordinate round-trips across widths and block
configurations, positionForStartOfLastLineBeforePosition: at
boundaries and when yOffset spans empty rows across a block,
and the May 2025 testConvertPositionMultiBlock scenario.

2026-05-25: Make right-gutter panels resizable by dragging
their left edge

The clippings and inline AI chat panels were fixed-width.
Add drag-to-resize to the right-gutter framework so both can
be widened or narrowed by dragging their left edge.

iTermRightGutterDividerView now subclasses
iTermDragHandleView (the same view the toolbelt uses for its
resize), so it provides its own mouse tracking and resize
cursor; the visible 1pt hairline is a left-pinned,
autoresizing sublayer over the wider transparent grab area.
The controller is the drag handle's delegate and applies the
width change using the toolbelt's incremental "return how
far the handle was allowed to move" contract, clamping so
the terminal grid keeps a usable minimum.

Per-panel widths persist under NoSync-prefixed user-default
keys (local UI state, not synced configuration) via
iTermGutterPanelWidths, which both the panels' width getters
and their registered widthProviders read so the layout
budget and the hosted view stay in sync. Widths are clamped
to [225, 1200]; the 225 floor keeps panels from getting
narrow enough to look broken.

2026-05-25: Dispatch workgroup peer/toolbar shortcuts
centrally

Custom peer-switch and toolbar (navigation/reload) shortcuts
were dispatched from PTYTextView.performKeyEquivalent:,
which bails when the text view isn't first responder. They
only fired when the terminal text view had focus; views like
the Diff overlay's Run Anyway button, the Code Review
prompt's text view, and the clippings table either lost the
shortcut or had to re-implement performKeyEquivalent: to
forward it.

Dispatch these shortcuts in
iTermApplication.handleKeypressInTerminalWindow:, the same
central key-down path the fixed lt-shift-cmd-digit peer
shortcut already uses, so they fire regardless of first
responder. Precedence is preserved: after user key mappings,
deferring to global key mappings, and beating the main menu
and profile bindings.

Removes the per-view special cases: PTYTextView's workgroup
block and textViewWorkgroupShortcutHandlesEvent:, the
workgroupShortcutHandler overrides in CodeReviewPromptView
and ClippingsView, and their plumbing through SessionView,
PTYSession+CodeReviewPrompt, and ClippingsGutterPanel.

2026-05-25: Session Status toolbelt: sort rows on initial
load

The viewDidMoveToWindow path populated the row array from
Dictionary.values without sorting, so rows appeared in
arbitrary order until the next status update (which goes
through the sorting code path).

2026-05-25: Add makefile target runbg to run the app in the
background

2026-05-19: API server: advisory-lock socket dir to prevent
second iTerm2 from clobbering

Two iTerm2 processes sharing an application-support
directory (same -suite, or both with no -suite) would race
in listenOnUnixSocket: the second to start unconditionally
unlink()s the path and bind()s its own socket, leaving the
first listening on an orphaned inode that no client can
reach. Take a non-blocking flock on private/socket.lock
before unlink+bind so the second instance refuses to start
the API server instead.

2026-05-19: OSC color overrides survive appearance and
separate-mode toggles

Fixes gitlab #12870. When a profile uses separate light /
dark colors, OSC 10 / 11 (and the other delegate-routed
color setters, OSC 4, 5, 12, 13, 17, 19, plus iTerm2's
SetColors and XTPOPCOLORS) were writing only the
appearance-matched suffixed profile key. A macOS appearance
switch then reloaded the other-mode key and silently
discarded the override.

screenSetColor:profileKey: now writes the base, Light, and
Dark variants of the target key, capturing baselines for all
three with an NSNull sentinel for variants the profile did
not previously have (a later OSC 110 / 111 reset propagates
NSNull through setSessionSpecificProfileValues' remove-key
semantics to un-invent the variants we wrote). The
mode-setting baseline is seeded the same way
screenSelectColorPresetNamed: does it so the reset path's
mode-transition logic stays correct.

screenResetColorWithColorMapKey:profileKey:dark: now
consumes all three related baselines in its no-transition
and "originally not separate, now is" branches. The
"originally separate, now not" branch prefers a real color
over an NSNull sentinel when picking the value for the
unsuffixed key so the brief intermediate state between the
color restore and the mode-setting restore does not flash
through a removed-key fallback.

screenRestoreColorsFromSlot: gets the same dual-write +
NSNull-sentinel + mode-baseline treatment, and stops
clobbering KEY_MATCH_COLOR with the saved background
(VT100SavedColorsSlot does not carry a match-highlight
color).

Adds tests/osc-color-persistence.sh, an interactive smoke
test that walks through OSC 10 / 11 / 12 / 17 / 4 /
SetColors / PopColors and verifies persistence across both
macOS appearance switches and the
separate-light-and-dark-mode profile toggle, plus the OSC
110 / 111 reset and the match-color fix.

2026-05-19: Color well: preserve source color space on edit.
Issue 12720.

2026-05-19: Review fixes for PR #666 custom AI headers

Validation and security - Validate header names against RFC
7230 token chars and reject
  values containing CR, LF, or NUL (closes a
  header-injection surface via user-supplied values).
- Expose AICustomHeaders.isValidName / isValidValue to ObjC
  (@objc class wrapping the former struct) so the settings
  UI can apply the same rules at edit time.
- Add Header alert validates name and value, refocuses the
  offending field on retry, and trims the name.
- Inline cell edits trim the name, reject invalid input with
an
  explanatory NSAlert, and drop the user back into editing
  the offending cell so they don't have to retype.
- Skip empty-name rows on save so blank rows do not
accumulate in
  the persisted plist.
- Disclaimer notes that values are stored unencrypted in
  preferences.

UI and structure - Move the custom-headers UI out of
programmatic AppKit code into
  PreferencePanel.xib: IBOutlets / IBActions, table view,
  add/remove segmented control (momentary, neither segment
  latched), Done button, and disclaimer label live in the
  XIB.
- Replace the popover host with a reusable titled sheet
(title
  bar hidden via NSWindowStyleMaskFullSizeContentView). The
  sheet NSWindow is created lazily once and reused so the OK
  button keeps its default-button (Return) registration
  across re-presentations. Hidden Escape button is added
  programmatically (XML 1.0 forbids U+001B in attributes).
- Drop the popover-only NSVisualEffectView wrapper from
  _manualAISettings.

Table view - Adopt the existing CompetentTableView from
  sources/Settings/CRUD.swift for Delete / Backspace
  handling; CompetentTableViewDelegate is now @objc so ObjC
  controllers can conform.
- Switch the table to view-based with NSTableCellView
prototypes
  and tableView:viewForTableColumn:row:; edits round-trip
  through controlTextDidEndEditing:, which falls through to
  super for notifications that aren't from this table so the
  base class's info.controlTextDidEndEditing dispatch still
  works (this was silently regressing
  kPreferenceKeyCustomScriptsFolder).

Enable state - Disable the entire section (table, segmented
control, cell text
  fields) when the master toggle is off;
  tableView:shouldSelectRow: blocks selection while
  disabled, and the minus segment is disabled whenever there
  is no selection or the feature is off.
- updateCoarseAIModelSettingsEnabled now gates the
"Configure AI
  Model Manually" button on _pluginOK &&
  [iTermAITermGatekeeper allowed] so users cannot open a
  sheet full of disabled controls.

Tests and docs - Add ModernTests/AICustomHeadersTests
covering toggle, override,
  validation, CRLF and NUL rejection, and empty value
  behavior.
- Release notes entry under AI Improvements.  - Drop
unrelated last-xcode-version bump.

2026-05-19: Add user-configurable custom HTTP headers for AI
backend requests

Adds a key/value table in the "Configure AI Model Manually"
popover where users can define extra headers (e.g. proxy
auth, routing tags) that are appended to every AI request
when the master toggle is on.  Custom headers override
built-in headers on collision (DLog warning is emitted).
Covers chat/completion, vector store, and file upload paths.

PR #666

2026-05-18: Open Quickly: stamp jump target on cross-window
MRU. Issue 12799.

Switch-to-session in Open Quickly went through
-[PseudoTerminal makeSessionActive:], which routed through
setActiveSessionPreservingMaximization: with
updateActivityCounter:NO.  The NO was inherited by analogy
from activateSessionInDirection: (where it's needed to
preserve cmd-[/] toggle semantics) but Open Quickly is an
explicit jump, not a toggle. The target session's
lastActivityOrdinal never got stamped, so Cmd-Shift-O Enter
could drift onto whichever sibling pane was active in the
destination tab.

Migrate Open Quickly to -[PTYSession reveal] and delete
makeSessionActive: (Open Quickly was the only caller). Going
through reveal also picks up disinter-buried-session
handling, workgroup peer-port swap, and explicit
setCurrentTerminal:, matching Open Quickly's named-mark
path. All other reveal callers are user-initiated jumps (URL
handlers, notifications, status bar, tmux dashboard, global
search, AI chat, etc.), so flipping
setActiveSessionPreservingMaximization: to
updateActivityCounter:YES is safe.

reveal originally selected the containing tab before calling
setActiveSessionPreservingMaximization:, which meant
tabView:didSelectTabViewItem: fired with the destination
tab's activeSession_ still pointing at the previously-active
sibling. didSelectTab then stamped that sibling on the MRU
ordinal in our place. Reorder so the active session is set
first; the redundant didSelectTab stamp now targets self.

2026-05-18: Don't steal IME Enter in AI Chat input. Issue
12867

ChatInputTextView.keyDown(with:) was intercepting "\r" and
routing it directly to the delegate's insertNewline submit
handler, bypassing AppKit's input-method client path. With
an IME composing (Chinese, Japanese, Korean, ...), pressing
Enter to commit a candidate was submitting the unfinalized
marked text instead of letting the IME finalize it.

Mirror the post-11610 ComposerTextView fix: drop the keyDown
override, move submit handling into insertNewline(_:), and
gate the delegate's doCommandBy:insertNewline: on the shift
modifier so Shift+Enter still falls through to insert a
literal newline.

2026-05-16: Claude Code install: cover sessions whose
profile was deleted

The trigger installer previously only updated sessions whose
source profile still exists in sharedInstance. Sessions
launched from a profile the user later deleted (or whose
original profile is missing after an arrangement reload)
were silently skipped because they cannot appear in the
picker and reloadProfile no-ops when the shared profile is
gone.

Detect these orphan sessions (live or buried, divorced or
not) and expose an opt-in checkbox in the picker, defaulted
on, that reports the count. On install, already-divorced
orphans get triggers written directly to their
sessionsInstance bookmark; non-divorced orphans are divorced
first so a target bookmark exists. Stale orphan bookmarks
with no live session are skipped so we don't accumulate dead
writes.

Also fix sharedProfileDidChange in the divorced path to
merge from the latest sessionsInstance bookmark rather than
the in-memory _profile, so external writers like the
installer aren't clobbered for keys in _overriddenFields.

2026-05-16: Share Claude Code workgroup builder with preset

The Claude Code installer and the Coding Agent + Diff + Code
Review preset were maintaining near-duplicate iTermWorkgroup
definitions.  Lift the structure into a parameterized
builder on WorkgroupPresets; the preset uses the defaults
(fresh UUIDs and a user-friendly name), and
ClaudeCodeWorkgroupTemplate calls the same builder with its
stable builtin.claudeCode.* IDs so existing triggers and
saved references keep resolving.

2026-05-16: Add Coding Agent + Diff + Code Review workgroup
preset

Mirrors the workgroup the Claude Code installer creates: a
Chat root with mode switcher + git status, a Diff peer
running git difftool with the changed-file selector and nav,
and a Code Review peer running claude in code-review mode.
Uses fresh UUIDs so the preset is independent of the
installer's stable-ID copy.

2026-05-15: Remove always-skipping test_openai_imageDescribe

The test had no body beyond a throw XCTSkip; OpenAI image
attachments go through ChatAgent's upload pipeline, not the
AILiveDriver path this class wraps.
AIRequestBuilderAttachmentTests covers the inline PDF path.
Drop the placeholder and the run_ai_live.sh entry that
matched it. If the ChatAgent end-to-end coverage gets
written, it belongs in a new test class anyway.

2026-05-15: Skip unreachable Gemini models in AILiveHarness
sweeps

gemini-2.0-flash and gemini-2.0-flash-lite are still listed
in AIMetadata so grandfathered keys keep working, but Google
rejects them for freshly-minted API keys (404 "no longer
available to new users" / 429 RESOURCE_EXHAUSTED on the
first call). The harness was exercising them anyway,
producing 16 spurious failures per sweep across multiTurn,
refusal, smoke, and toolCall scenarios.

Move the existing AIMetadataFixtureCoverageTest skip list
onto AILiveHarness as unreachableForNewKeys, and have the
harness's default model lookup filter against the same set.
An explicit ITERM2_AI_LIVE_<VENDOR>_MODELS override still
targets them deliberately for anyone with a grandfathered
key.

After this, tools/run_ai_live.sh gemini passes 8 of 8 cases.

2026-05-15: Live-AI test plumbing: marker-based gate,
recaptured fixtures

Replace the awkward mtime freshness check on AILiveHarness's
config file with a clean inter-script signal:

* run_tests.expect deletes a stale config file at startup
unless its
  parent sets ITERM2_AI_LIVE_KEEP_CONFIG=1. Plain
  ModernTests runs thus always start clean; AILiveHarness
  sees no config and skips.
* run_ai_live.sh exports the marker before invoking
run_tests.expect
  so its just-written config survives the cleanup. It also
  stops using `exec` for run_tests.expect  bash needs to
  stay alive long enough for its EXIT trap to remove the
  config after tests finish.  That `exec` was the original
  source of leaked stale configs.
* AILiveHarness.swift returns to the simple "file exists?"
gate.

Refusal-parser tests: stop skipping captures whose response
was an HTTP error. The fixture is supposed to be a real
refusal payload, so fail loudly when it isn't, and recapture
instead. Five 429/404/503 Gemini captures are recaptured
here:

  - gemini-3-pro-preview {stream, noStream}: fresh
  successful refusals
    committed.
  - gemini-2.0-flash {stream, noStream}: Google now returns
  429
    RESOURCE_EXHAUSTED for this model on a fresh API key
    (same practical state as gemini-2.0-flash-lite, already
    in the skip list). Move it into deprecatedToNewKeys with
    a comment about the quota wall. The previous fixtures
    are still removed so the parser tests don't see
    error-shaped data.
  - gemini-2.0-flash-lite {stream, noStream}: previous
  captures were
    also 404 errors. Removed; coverage was already exempted.
  - gemini-2.5-flash stream: previous capture was 503.
  Removed; the
    noStream sibling covers the model.

After this: 2207 passed, 53 skipped (AILiveHarness on
demand), 0 failures.

Co-Authored-By: Claude Opus 4.7 (1M context)
<noreply@anthropic.com>

2026-05-15: Make full ModernTests run green by default

A `tools/run_tests.expect ModernTests` sweep flagged 13
failing cases that were polluting the signal  none related
to product code, but all visible as red. Cleanup so the
suite is honest:

* AILiveHarness was gating itself on "config file at known
path
  exists." A wrapper invocation killed mid-run (SIGKILL,
  aborted xcodebuild) leaves the file behind; the next
  regular ModernTests run picks it up, blows real API
  budget, and shows nine red cases when vendor keys have
  rotated or the script never ran in this session at all.
  Add a 60-minute freshness window on the config mtime so
  stale state can't masquerade as a live invocation. Fresh
  writes from `tools/run_ai_live.sh` are well within the
  window; everything else skips cleanly.

* AISafetyRefusalParserTests was failing on three Gemini
fixtures whose
  captured response was an HTTP error (429
  RESOURCE_EXHAUSTED, 404, 503) rather than a refusal body.
  The fixture is a record of the API call that day, not a
  refusal payload to validate. Skip captures whose
  `response.error` field is set in both the non-streaming
  and streaming variants. Real refusal data still exercises
  the parser.

* PathTests.testApplicationSupportDirectory_DefaultSuite
(and the
  WithoutCreating sibling) asserted the path ended in
  `/iTerm2`, but the test runner sets `customSuiteName =
  "iterm2-tests"` so the path ends in `/iterm2-tests`. The
  test name is misleading  there is no "default suite"
  during test runs  and the assertion was wrong about what
  it was checking. Read customSuiteName and compare against
  the actual suffix.

After this: 2207 passed, 53 skipped (AILiveHarness on
demand), 0 failures.

Co-Authored-By: Claude Opus 4.7 (1M context)
<noreply@anthropic.com>

2026-05-15: Unbreak iTerm2XCTests so
PTYTextViewAccessibilityTest can run

The iTerm2XCTests target had been failing to build for a
while behind a "Module 'BetterFontPicker' not found" error
in unrelated test files; fixing that exposed a stack of
further bit-rot. Two kinds of changes:

* Test sources adapted to current production APIs:
  - VT100GridTest.m: screen_char_t.underlineStyle was split
  into bit
    fields; use ScreenCharGetUnderlineStyle.
    moveCursorDownOneLine...  grew a sentToLineBuffer: out
    param.
  - iTermRuleTest.m: -scoreForHostname:... gained
  commandLine: and
    expressionValueProvider: parameters; pass nil at every
    call site.
  - iTermPasteHelperTest.m: warning block had a non-void
  return path
    missing.
  - iTermTextExtractorTest.m, SearchTests.m:
  StringToScreenChars()
    grew an rtlFound out param. SearchTests.m also
    referenced a deleted iTermCharacterBufferContext class;
    drop the setUp call.

* Project plumbing the test scheme needs to build the tree:
  - iTerm2XCTests config: add BetterFontPicker/ColorPicker/
    SearchableComboListView to FRAMEWORK_SEARCH_PATHS so the
    iTerm2SharedARC-Swift.h module scan can find them.
  - iTerm2ForApplescriptTesting config: add sources/** to
    HEADER_SEARCH_PATHS and pull iTermSwiftPackages +
    librailroad_dsl into Frameworks + LIBRARY_SEARCH_PATHS
    so it can at least link.
  - iTerm2Tests target: drop its now-redundant dependency on
  the
    legacy iTerm2ForApplescriptTesting app (which still hits
    a code sign issue), and pick up the missing
    AppleScriptTest.m source plus the iTermSwiftPackages and
    librailroad_dsl link inputs and search paths directly so
    the test runner builds standalone.

After this, `xcodebuild test -scheme iTerm2Tests
-only-testing:iTerm2XCTests/PTYTextViewAccessibilityTest`
passes all three cases.

Co-Authored-By: Claude Opus 4.7 (1M context)
<noreply@anthropic.com>

2026-05-15: make run: activate the launched dev iTerm2 and
forward Ctrl+C

So that a make run launch reliably comes to the foreground
even when another iTerm2 is already running, while Ctrl+C
still kills the dev process.

2026-05-14: Add Diff workgroup session mode

Diff sessions defer their command until the workgroup's git
poller reports staged or unstaged changes. The Claude Code
workgroup's Diff peer and the Coding Agent + Diff preset opt
in, so neither runs difftool on a clean tree. A waiting
overlay explains the state and exposes a Run Anyway button;
on reload over a clean tree a queued- reload variant shows
alongside the previous output with a Cancel button. Gitbase
changes that leave the new base empty also surface the
overlay, and the deferred closure resolves gitBase at fire
time so changes during the wait propagate without
rebuilding.

Existing Claude Code workgroups are migrated to .diff on
first launch via a NoSync-latched one-time backfill in
iTermWorkgroupModel.

2026-05-14: Fix main-thread stall on large output bursts for
VoiceOver

Two changes in the accessibility output-settle path:

* LineBlockMetadataArray's provider struct held a heap block
that
  captured self for copy-on-write. The inner loop in
  -[LineBlock locationOfRawLineForWidth:lineNum:] created
  and released one such block per entry; on a settle that
  touched thousands of lines, the Block_release path through
  objc_destructInstance and the associated-objects DenseMap
  dominated the sample. Replace the block with an unretained
  back-pointer to the array and call -willMutate directly
  from the mutable accessor.

* -[PTYTextView accessibilityAnnounceNewOutputAfterSettle]
read every
  line between the saved cursor and the current cursor, then
  handed VoiceOver one giant string that it dropped or
  truncated anyway. Cap the announcement to the most recent
  256 lines and adjust the firstAbsoluteLine passed to
  +accessibilityAnnouncementLinesForTrimmedLines: so the
  existing cmd-line strip naturally falls through.

Co-Authored-By: Claude Opus 4.7 (1M context)
<noreply@anthropic.com>

2026-05-14: Clippings panel: stop bloating zoomed windows

In rightExtraDidChangeImpl, the previous atRightEdgeBefore
check used a 0.5pt threshold and was a binary gate, so
windows the user zoomed to fit the display (off by a
fraction of a pixel from character-grid snapping) fell
through and grew. Match the toolbelt's pattern: short-
circuit when the window is within 10pt of the screen's right
edge with no prior growth to undo, otherwise compute the
ideal frame and clamp growth to not exceed the screen edge
and cap shrinkage at what we previously grew. Accumulate the
realized delta after setFrame: so discrepancies from
constrainFrameRect: and canonicalFrameForScreen: don't drift
across toggles.

2026-05-14: Route workgroup shortcuts from clippings panel

The clippings table view claimed ^1/^2/etc. via keyDown
before PTYTextView's performKeyEquivalent could route the
event to the workgroup handler. PTYTextView bails from
performKeyEquivalent when it isn't first responder, so
nothing else in the hierarchy walk picked up the shortcut.
Forward the event from ClippingsTableView's
performKeyEquivalent to the host session's
textViewWorkgroupShortcutHandlesEvent, resolved through the
gutter panel's currentSession so peer swaps still hit the
live host.

2026-05-14: Claude Code install: also walk live sessions for
divorce match

2026-05-14: Route workgroup shortcuts from Code Review
prompt overlay

2026-05-13: Add clippings history with archive button and
back/forward

The clippings panel grows an archive button and a
back/forward segmented control so a code-review-then-fix
workflow can keep previous rounds of findings around. The
matching it2 session archive-clippings command and
iterm2.archive_clippings built-in function let Claude Code
archive automatically at the start of each review.

2026-05-13: PSMTabBarCell: keep subtitleString populated
across style changes

setStringValue: was dropping the subtitle whenever the
current style's supportsMultiLineLabels was NO, merging
"title subtitle" into stringValue and leaving subtitleString
nil. If the style or bar height later changed such that
multi-line was supported, the cell still had no subtitle to
draw.

Always split title/subtitle into the two ivars and let
Yosemite-style recompose them inline at draw time when its
bar is too short for a separate subtitle line. Tahoe and
Minimal at their normal heights draw them stacked as before.

Surfaced by the new "Show status in tab subtitle" preference
when switching from Compact to Minimal mid-session, but the
same stale-state issue applied to any caller of
setStringValue: with a multi-line label under a single-line
style.

2026-05-13: Add a preference to hide session status from the
tab subtitle

The colored dot on a tab already conveys session state
(working / waiting / idle). The text under the title
duplicates that signal and can clash with custom tab colors.
Add a preference, default ON, that gates whether
session-status text (from cc-status, OSC 21337, the
SetTabStatus trigger, or iterm2.set_status) is woven into
the tab subtitle and allowed to tint its color. The dot, the
toolbelt entry, and the writers themselves are untouched.

The toggle lives in the gear popover of the Session Status
toolbelt tool.  Flipping it posts a notification so open
tabs refresh their labels live.

2026-05-12: Make refusal fixture refresh opt-in

Refusal scenarios used to overwrite the on-disk fixtures
under ModernTests/Resources/SafetyRefusalFixtures/ on every
live run, which dirtied the working tree with
non-deterministic noise (model wording, request IDs,
latency, token counts) any time the suite ran. The fixtures
are still produced when intentionally refreshing them: set
ITERM2_AI_LIVE_REFRESH_REFUSAL_FIXTURES=1 in the env that
tools/run_ai_live.sh forwards to opt in.

2026-05-12: Fix Anthropic 400 when stray message splits
tool_use/tool_result

AnthropicRequestBuilder now reorders converted messages so a
tool_use block in an assistant message is always immediately
followed by a user message carrying the matching
tool_result, with any intervening messages preserved after
it. The Cockpit's racy persistence could emit a user-text
status update between a tool_use and its tool_result; that
ordering survived to the wire and Anthropic rejected it.

2026-05-12: Make Claude Code onboarding intro a real sheet

The "Before You Start" intro was a subview overlay on the
installer panel, not a real modal sheet, so the panel's
Install/Next button still owned the window's default-button
slot and Enter routed to it instead of Continue. Present it
as an actual NSWindow attached via beginSheet so the parent
is truly inert while the intro is up.

2026-05-12: Remove Explain Status step from Claude Code
onboarding

The step's NSPopover could throw if the matched Claude
session lived in a window whose toolbelt subview was still
hidden (e.g., when the prior Show Toolbelt step had to
launch a window asynchronously and the user advanced before
its callback applied toolbelt visibility).  Dropping the
step also shortens the setup flow.

2026-05-12: Add global Open Quickly hotkey

A new shortcut in Settings > Keys lets you summon the Open
Quickly window via a system-wide hotkey while another app is
in front, without activating iTerm2 until you commit a
selection.

Show path (modeled on iTermProfileHotKey's floating panel,
which has the same constraint): - styleMask OR'd with
NSWindowStyleMaskNonactivatingPanel.  - canBecomeKeyWindow =
YES, hidesOnDeactivate = NO.  - collectionBehavior =
CanJoinAllSpaces | FullScreenAuxiliary |
  IgnoresCycle. Transient is omitted because transient
  windows "do not contribute to the active state of the
  app", which prevents the window server from re-painting
  the panel on macOS 26.
- level = NSMainMenuWindowLevel - 2.  - Show via
makeKeyAndOrderFront:. Use orderOut: (never close) for
  dismissal so the window-server doesn't refuse re-show.
- When iTerm2 is in the background, set alphaValue=0, then
  makeKeyAndOrderFront:, then animator.alphaValue=1. The
  alpha transition is what forces the window server to
  actually paint the non-activating panel; without it the
  panel becomes key but stays invisible on macOS 26.

Commit path: - iTermOpenQuicklyHotKeyProvider (Swift)
registers the user's
  shortcut with iTermCarbonHotKeyController and shows Open
  Quickly on fire. Re-registers on every -invalidate so
  changing the shortcut unregisters the old combo first.
- openSelectedRow uses NSWorkspace.frontmostApplication (not
  NSApp.isActive, which lies while our panel is key) to
  decide whether to activate iTerm2 before
  makeSessionActive:.
- If the destination session lives on a different Space,
macOS
  sometimes picks a different app as frontmost when the
  Space switch animation completes. Register a one-shot
  observer for NSWorkspaceActiveSpaceDidChangeNotification
  and re-activate iTerm2 two runloop spins after the Space
  change.

Preferences: - Three new sync-able keys
(OpenQuicklyHotkeyCode/Char/Modifiers).
  All zero means unbound; no separate enabled-flag.
- A new advanced setting `openQuicklyAnimationDuration`
(default 0.12s)
  controls the existing height-grow animation in the Open
  Quickly window. Set to 0 to disable.

KeysPreferencesViewController binds the prefs to the new
iTermShortcutInputView in the Keys tab, and -invalidate
fires on the provider after every shortcut change.

Issue 12799.

2026-05-12: Support DeepSeek v4 thinking mode end-to-end

DeepSeek v4 requires reasoning_content to be echoed back on
every subsequent assistant turn when thinking is enabled, or
it returns 400 on multi-turn tool calls. Plumb the field
through the parser, request builder, persistence layer, and
AIConversation accumulator so reasoning round-trips
correctly for both streaming and non-streaming runs and
across reloaded chats. Add .configurableThinking to the v4
models so the toolbar toggle propagates; scope the thinking
block to only those models so older DeepSeek endpoints don't
see an unknown key.

Harvest streamed reasoning into Message.agentReasoning at
both the text chunk and commit boundaries (covers the
reasoning-only stream that delivers no text chunks). Drop
the redundant Codable boilerplate in
DeepSeekRequestBuilder.Body.Message (Swift's synthesis
already does encodeIfPresent for Optional fields), and make
reasoning_content structurally impossible to encode from
CompletionsMessage by moving the key out of the encode-side
CodingKeys. AITermController now iterates over
choiceMessages so parsers can surface multiple messages from
one delta (reasoning + tool_calls in the same chunk).

Companion offline tests in AIParserCombiningTests and the
new AIChatMessagePersistenceTests; live harness adds
thinking smoke, tool-call round-trip, plain-text
AIConversation multi-turn (with auth header verification so
we don't silently pass against a keychain singleton key),
and an empirical reasoning-only round-trip test that
confirms DeepSeek accepts the content=""+reasoning_content
shape.

2026-05-12: Clear tab status when a session's program
terminates

Drop the cached iTermSessionTabStatus too, since its
sessionID is captured at init and would otherwise stay
pinned to the pre-restart guid after
replaceTerminatedShellWithNewInstance regenerates it,
causing later set-status updates to be keyed under a stale
id.

2026-05-12: Xcode wants to make love to this xib, so let it

2026-05-12: Focus prompt text field on Code Review peer
activation

PTYTab.setActiveSession's makeFirstResponder call ran after
swapSession:withBuriedSession: and reclaimed focus from the
CodeReviewPromptView's text view set in viewDidMoveToWindow.
mainResponder now returns the overlay's text view while the
overlay is on screen.

2026-05-12: Add per-peer jump shortcuts to workgroups

Each peer in a workgroup can now carry an optional keystroke
that activates it from any session in the workgroup. Unset
peers fall back to the built-in opt-shift-cmd-number
binding. Shortcuts route through the same dispatch as
workgroup toolbar shortcuts, so they beat profile bindings
and the main menu but defer to global key bindings. The mode
switcher segments render the configured shortcut next to
each peer's label when set.

2026-05-12: Fix Code Review prompt overlay appearance drift

The overlay snapshotted windowBackgroundColor.cgColor
against whatever NSAppearance.current happened to be at init
and viewDidChangeEffectiveAppearance time, which is not
guaranteed to match the view's effectiveAppearance. The text
view drew correctly because AppKit sets NSAppearance.current
for the drawing path, so the panel and its text view could
disagree on light/dark.

Inherit from iTermLayerBackedSolidColorView so the CGColor
is refreshed in updateLayer, where AppKit sets the drawing
appearance correctly.

2026-05-12: Honor option-modified "Quit and Close All
Windows". Issue 12852

Sample the option modifier at entry to
applicationShouldTerminate: and, past the confirmation
prompt, set a new forceDiscardState flag on
iTermRestorableStateController. The flag vetoes the existing
NSQuitAlwaysKeepsWindows-OR-shouldRestoreStateOnNextLaunch
logic so option-quit can finally erase saved state. Only
triggers when the standing pref would otherwise force save,
so option-modified "Quit and Keep Windows" (pref off) keeps
the user override. Committing past the cancel branch ensures
a canceled quit does not leave the flag set across attempts.

2026-05-12: Pass Cloudflare Turnstile and fix browser
geolocation handling. Issue 12865

In challenge frames (Cloudflare Turnstile, hCaptcha, Arkose,
FunCaptcha, Google reCAPTCHA), inject a cloak first in the
page world that strips window.webkit and signals the
page-world bridges to skip installation. This hides the
iTerm-prefixed message handlers and avoids wrapping console,
Notification, navigator.geolocation, AudioContext, Audio,
document.createElement, and HTMLMediaElement.prototype.play,
so the captcha probe sees an unmodified Safari surface.

Also fix three pre-existing geolocation bugs: page hangs
forever when permission is not granted (now sends
PERMISSION_DENIED so the errorCallback fires); replies sent
on the main frame missed iframe callers (LocationRequest and
WatchRequest now carry the originating WKFrameInfo);
system-level denials were persisted as per-site decisions,
locking sites out forever (only an explicit Allow or Block
in the per-site dialog is now remembered). Same persistence
fix applies to notification permissions.

2026-05-12: Read haveScrolled from mutable grid in
VT100Screen sync

The immutable _state.currentGrid is built by
copyMiscellaneousStateTo:, which never propagates
_haveScrolled, so the sync result was effectively pinned to
NO and PTYTextView's scroll-driven full repaint path was
suppressed. Capture the value from mutableState.currentGrid
before didSynchronize: clears it.

2026-05-12: Invalidate only changed lines in the legacy
renderer

The legacy (non-GPU) renderer was marking the whole text
view dirty on any cell update because setNeedsDisplayOnLine:
and the other per-line invalidation entry points all
funneled into requestDelegateRedraw, which passed
self.bounds. Add a requestDelegateRedrawInRect: that
translates from PTYTextView coords to legacy-view coords via
virtualOffset, plumb it through
PTYSession.textViewNeedsDisplayInRect: and a new
SessionView.requestRedrawInRect:, and route the per-line
entry points (setNeedsDisplayOnLine:, setCursorNeedsDisplay,
invalidateInputMethodEditorRect, the blink scanner, and the
updateDirtyRects per-line loop) through it.

Apply the existing rectWithHalo: 1-row halo on the
invalidation side so descenders spilling out of a dirty line
into N+1 (and leftover descender pixels from the previous
frame) are cleared and repainted. The drawing helper's
existing draw-range widening handles the inverse case
(descenders from N-1 intruding into N) without changes, by
drawing wider than the dirty rect and letting AppKit clip
the output.

A scroll shifts virtualOffset out from under any pending
small dirty rect that was already enqueued in the legacy
view. When updateDirtyRects: sees haveScrolled it falls back
to a full requestDelegateRedraw so AppKit unions it with
anything pending and the visible area is reconciled.
Per-line search-result and highlight removal stays inside
the loop so a single-line scroll doesn't wipe matches on
lines that merely shifted up.

The IME invalidation tracks the previously invalidated rect
(in PTYTextView coords, pre-halo) and falls back to a full
redraw whenever the new rect doesn't contain the previous
one, catching multi-row compositions that shrink, cursor
moves that shift the IME, and the empty-composition (full
clear) case.

Add an advanced setting showDirtyRectsInLegacyRenderer that
flashes the dirty rect red and outlines it in a random color
for visually verifying which regions are repainted per
frame.

Add tests/spinner.sh (Claude Code-style baseline workload)
and tests/redraw-edge-cases.sh (descenders, ligatures,
bismillah, VS15/VS16, italics, combining marks, wide CJK,
box drawing) to stress the per-line invalidation path
against the historical hazards that originally forced
whole-view redraws.

Issue 12790.

2026-05-12: Wrap Anthropic tool_use ids so persisted tool
calls round-trip

The Anthropic parser stored the tool_use id only on the
inner LLM.FunctionCall.id, leaving the outer
LLM.Message.Body.functionCall wrapper nil. The serializer's
.functionOutput branch reads the tool_use_id from the
wrapper, so reloaded function outputs were sent as plain
text and Anthropic rejected the request for a missing
tool_result block. Both sides now agree on the wrapper, and
itemID mirrors the tool_use id so parallel tool calls keep
distinct identities in tryAppend.

2026-05-12: Honor IT2_SUITE when checking
disable-automation-auth in iterm2 py lib

External scripts targeting a -suite instance previously read
the stock ~/Library/Application
Support/iTerm2/disable-automation-auth, so users who had
clicked Always Allow All Apps in stock iTerm2 would skip the
AppleScript cookie flow and then get a silent 401 from the
suite server (which has no auth-disable file of its own).
Match the existing IT2_SUITE pattern already used by
connection.py._unix_domain_socket_path.

Issue 12864.

2026-05-12: Open Quickly improvements.  Issue 12799.

* Change how "go to session" items in open quickly are
scored to be in MRU order and exclude current session.  *
Fix empty titles for menu items in Open Quickly with empty
query * Add session preview side panel to Open Quickly

2026-05-11: Stop re-encoding -suite name in daemon socket
filename

The application-support directory and the dotdir fallback
are already per-suite, so writing daemon sockets as
{suite}-daemon-N.socket double-encoded the suite and broke
orphan adoption: the discovery glob and the basename parser
still looked for the plain iterm2-daemon- prefix. Socket
files now match those locations. A transitional fallback in
pathForNumber: and the orphan adopter still accepts the
{suite}-daemon-N.socket form so beta users don't lose live
daemons across the upgrade. Issue 12860.

2026-05-11: Match symbolic hotkey modifiers exactly, not by
subset

The check in iTermSymbolicHotkeys.haveBoundKey used a subset
test on the modifier bits, so a system shortcut bound to
Ctrl+Right (or any strict subset of an event's modifiers)
also intercepted Ctrl+Shift+Right. That caused iTerm2 to
defer the keystroke to macOS and drop the terminal output.
Compare equality on the relevant modifier bits (shift,
control, option, command, function), ignoring CapsLock,
NumericPad, and device-specific left/right bits. Issue
12859.

2026-05-11: Guard +[NSData dataWithBase64EncodedString:]
against nil

A nil input flowed through to apr_base64_decode_len(NULL),
which dereferences the pointer and crashes. The trigger is a
saved status bar layout whose RPC-provided text component is
missing the registration-request v2 string, so castFrom:
hands nil to the decoder during launch. Also guard the
unchecked addObject: in iTermStatusBarLayout so a nil
component from a failed init does not raise
NSInvalidArgumentException. Issue 12861.

2026-05-11: Back the fake title bar with a titlebar-material
VEV in Compact

The Compact tab style returns clearColor for the selected
tab's background and relies on iTermTabBarBacking's
NSVisualEffectView to provide the material. When the fake
title bar replaces the tab bar (compact window with one
tab), no such VEV existed and the title strip went fully
transparent on top of the now-clear window background. Add a
sibling NSVisualEffectMaterialTitlebar view just below
_titleBackgroundView, scoped to TAB_STYLE_COMPACT so Minimal
and other styles are unaffected. Issue 12601

2026-05-11: Coalesce iTermGenericEvaluator re-evaluations
via IdempotentOperationJoiner

Each call to setNeedsReevaluation previously enqueued a
fresh dispatch_async on the main queue, so a tmux -CC pane
title that updated many times per second piled millions of
__NSMallocBlock__ allocations onto the main queue and
exhausted memory. Route the deferred update through
IdempotentOperationJoiner so at most one reevaluation is
pending per evaluator, and drop the now-redundant
needsReevaluation flag. Issue 12857.

2026-05-11: Fix crash parsing API invocations containing
non-BMP characters

OptionalPathRecognizer indexed by Unicode scalar while
CoreParse's tokenPosition is in UTF-16 code units, so after
a quoted string with emoji the offset could run past
scalars.endIndex and trap. Index in UTF-16 instead. Issue
12856.

2026-05-11: Remove vertical window-size clamp in
windowWillResize

The MIN(maxFrame.height, proposed.height) clamp silently
rejected vertical resizes when the window's origin.y was
below visibleFrame.minY (window straddling the screen edge),
because there was room to grow but the clamp didn't account
for the window's position. The matching width clamp was
removed in 230c960c3 (2011); height was left behind as an
oversight.

2026-05-11: Fade session toolbar backing to match window
transparency

The NSVisualEffectView under the session toolbar uses
.withinWindow blending, so at low window alpha it sampled
the transparent session backing and read opaque on top of a
see-through window. Drop its alphaValue in step with the
session's transparencyAlpha so the desktop shows through the
toolbar like the rest of the pane.

2026-05-11: Refactor AI parser/builder layer and add live
test harness

Bug fixes surfaced by end-to-end vendor testing: - OpenAI
Responses non-streaming function call now chains via
  previous_response_id so the reasoning item is not lost.
- Gemini thoughtSignature is preserved through
function-call+text
  body merges.
- DeepSeek non-streaming tool_calls are no longer dropped by
the
  parser.
- Anthropic [text, tool_use] preambles round-trip with the
function
  call instead of being silenced.
- OpenAI Responses CodeInterpreterToolCall accepts outputs
(not
  results) per the current wire shape.
- Chat-completions request uses tools/tool_choice; legacy
functions/
  function_call mixing produced 400s on modern endpoints.
  Release- notes call-out added for users pointing iTerm2 at
  older self-hosted OpenAI-compatible servers.
- DeepSeek multipart body chooses the assistant tool-call
shape over
  the tool-output shape when a future parser produces both.

Parser-level combining (replaces the ad-hoc combine logic
that used to live in
AITermController.parseNonStreamingResponse):

Each non-streaming parser now returns at most one
LLM.Message per response. Turns that the vendor splits into
multiple pieces (Anthropic content blocks, OpenAI Responses
output items, Gemini parts, modern chat-completions text +
tool_calls) collapse into a single .assistant Message with a
multipart body. AITerm sees a uniform shape and no longer
needs to reconcile sibling messages or sacrifice preamble
text across role boundaries.

Why: choiceMessages was conflating two semantics. For
Anthropic / Responses / Gemini-parts it carried consecutive
pieces of one turn; for OpenAI chat-completions / DeepSeek /
Gemini-candidates it carried n-sampling alternatives.
Combining all of them in AITerm was only safe by coincidence
(iTerm2 never sets n>1). The platform-specific shape now
lives in the parser layer, where the wire shape is known.

Parsers (clamp to first candidate; multipart for split
turns):
  CompletionsAnthropic.swift  content blocks -> multipart
  .assistant ResponsesAPIResponse.swift  output items   ->
  multipart .assistant
			      (function call now embedded in
			      the assistant turn rather than
			      carried in a sibling
			      .function-role Message)
  Gemini.swift                candidates.first; parts ->
  multipart LegacyOpenAI.swift          choices.first
  LLMModernProtocol.swift     choices.first (non-streaming
  and
			      streaming);
			      CompletionsMessage.
			      llmMessage returns multipart
			      when both content and
			      tool_calls are set
  DeepSeek.swift              choices.first on streaming

Multipart-aware accessor:
  LLM.Message.function_call walks .multipart parts so AITerm
  can detect a function call inside a collapsed turn without
  inspecting the body shape (mirrors what functionCallID
  already does).

Round-trip:
  ResponsesAPIRequest.transform(message:) now returns
  [ItemListEntry] rather than ItemListEntry? and uses a new
  assistantEntries helper to fan out an .assistant multipart
  turn into a (message, function_tool_call) pair on the
  wire. The previousResponseID==set path still strips all
  but the last item, so chained conversations are unchanged;
  the previousResponseID==nil full-resend path now
  reconstructs the wire shape correctly.

AITerm simplification:
  parseNonStreamingResponse takes choiceMessages.first,
  checks the multipart-aware function_call, and dispatches.

Backward compatibility (persisted chat-history Messages):
  LLM.Message / Body Codable surface is unchanged
  (CodingKeys, encode, init(from:) and the case set all
  untouched), so chats serialized before this commit decode
  the same. Old persisted shapes (separate .assistant text +
  .function-role function call Messages) still round-trip
  through the existing branches in every request builder.
  assistantEntries handles each .attachment subtype for
  assistant turns explicitly: .code inlines as text, .file
  and .fileID emit a name/mime placeholder plus a DLog
  (Responses' assistant role can't accept input_file on the
  wire), .statusUpdate is ephemeral and dropped, and
  .functionOutput/.uninitialized fall back to maybeContent
  so persisted Messages with malformed-but- grandfathered
  shapes survive resend.

Infrastructure: - AILiveHarness: 39 test methods covering
smoke, multi-turn, tool
  call, and refusal scenarios across OpenAI / Anthropic /
  Gemini / DeepSeek plus synthetic chatCompletions and
  legacyCompletions.
- AILiveDriver drives the harness via AITermController,
captures raw
  wire data through iTermAIClient.liveObserver, and retries
  on transient HTTP statuses (429, 5xx, RESOURCE_EXHAUSTED,
  timeouts).
- iTermAIClient.liveObserver is always compiled in (not
guarded by
  ITERM_DEBUG) and is backed by MutableAtomicObject so
  concurrent observer install/uninstall is safe.
- tools/run_ai_live.sh wraps run_tests.expect with a TMPDIR
config
  file so live tests opt in explicitly and the default
  ModernTests run still skips them.

Offline regression coverage: - AISafetyRefusalParserTests
replays 66 captured refusal fixtures
  (one streaming + one non-streaming per model) through each
  vendor parser.
- AIMetadataFixtureCoverageTest fails if a new model lacks
fixtures
  (with a deprecated-model skip list).
- AIRequestBuilderAttachmentTests covers Anthropic
image/multipart,
  Gemini inlineData, OpenAI PDF, and DeepSeek text
  attachments.
- AIParserCombiningTests covers the new combining logic:
multipart-
  aware function_call detection, Anthropic [text, tool_use]
  and Gemini multi-part combining (with thoughtSignature
  preservation), modern chat-completions text + tool_calls
  combining (streaming and non-streaming), DeepSeek
  streaming tool calls and end-of-call sentinel, OpenAI
  Responses message + function_tool_call collapsing,
  ResponsesAPIRequest round-trip for multipart, flat text,
  the four assistant-side .attachment shapes, and the
  first-choice clamps on every parser.

Verified all four vendors' live tool-call non-streaming +
streaming tests pass against real APIs.

2026-05-11: Route first-party prefs through
iTermUserDefaults so -suite works

Migrate composer height, recent tab colors, and the local
network permission prompt version from standardUserDefaults
to iTermUserDefaults.userDefaults() so they honor a custom
suite. Leave kCPKUseSystemColorPicker alone since
ColorPicker reads it from standardUserDefaults directly and
has no awareness of -suite. Also fix
iTermURLActionFactory.h, which inherited from NSUserDefaults
instead of NSObject.

2026-05-11: Run ModernTests against a separate UserDefaults
suite

Pass -suite iterm2-tests to the test host so
iTermUserDefaults reads and writes go to iterm2-tests.plist
instead of the live app's domain, preventing a running
iTerm2 from hanging on prefs notifications during test runs.

2026-05-10: Floor faint-text alpha at 0.1 so SGR 2 text
never goes invisible

A value of 0 collapses processedTextColorForTextColor's
alpha composite to the background color exactly, making
faint text indistinguishable from the cell background. Clamp
the slider in Profiles > Colors and the colormap setter, and
fix the slider's tooltip (it was a copy of the
minimum-contrast tooltip).

2026-05-10: Fix vertical alignment of lock icon in edit
session view

2026-05-08: Backfill default toolbar shortcuts on existing
workgroup configs

iTermWorkgroupModel runs a one-time migration on first
launch that stamps the default ctrl-cmd-left /
ctrl-cmd-right / cmd-R shortcuts onto pre-existing
.navigation and .reload toolbar items whose shortcuts are
still empty (the only state any item could have been in
before this feature shipped). The latch is kept in NoSync
user defaults so a deliberate clear by the user isn't
re-seeded on the next launch. Items whose navigation cluster
has at least one shortcut already set are left alone.

2026-05-08: Add keyboard shortcuts to workgroup toolbar
items

The navigation cluster (back/forward/reload) and the
standalone reload toolbar item each carry an optional
shortcut, configurable inline in the workgroup session
detail view. Defaults are ctrl-cmd-left, ctrl-cmd-right, and
cmd-R; tooltips on the buttons surface the current binding.
Dispatch hooks PTYTextView's performKeyEquivalent ahead of
the main menu so toolbar shortcuts beat profile bindings and
menu items, while still deferring to global key bindings.

2026-05-08: Make the heavy window border look better by
using the corner detection feature we have for active
session border

2026-05-08: Add Erase All Settings and Data button. Issue
12852

Adds a discoverable way to fully reset iTerm2 to a clean
state without resorting to manually deleting SavedState. The
button in Settings > General > Settings offers to export
first, then on confirmation walks the same
ImportExportConfig entity list as the existing export/import
flows so any future entity automatically participates in
erase. Wipes preferences (main and private suites), secure
settings, the dot-dir, the application support folder, the
custom or default scripts folder, and the macOS Saved
Application State directory, then calls _exit(0) so no
teardown path can re-save state on the way out.

A dry-run advanced setting routes every state mutation
through an EraseAction wrapper that NSLogs what it would do
instead, leaving the app running so the feature can be
tested without losing data.

2026-05-08: Fix issues in right-side tab position

- Add missing sessionsAtRight to PTYTab so dragging out a
right-tab
  session doesn't crash with unrecognized selector.
- Include PSMTab_RightTab in imageFromSelectedTabView's
image-sizing
  switch so the drag-out image isn't truncated.
- Fix sign of return value in dragHandleView:didMoveBy: for
the
  right-side bar; the protocol expects the handle's actual
  movement, not the width delta, so resize accelerated off
  into space.
- Rename leftTabBarDragHandle to verticalTabBarDragHandle
since it
  serves both side bars.
- Add a zero-width space to the "Right" menu title so macOS
Tahoe
  stops auto-decorating it with the system tile-window
  shortcut.
- Shift the new-window x offset for right-side drag-out so
the tab
  cell stays under the cursor instead of trailing far to the
  right.

2026-05-08: feat: add right-side tab position (#660)
2026-05-08: Add per-profile contextual alternates toggle.
Issue 12847

In 3.7 the default Core Text ligature level was bumped to 2
to activate calt-based ligatures for fonts like Monaspace,
Cascadia Code, and Hasklig. The downside is that calt also
carries surprising substitutions (e.g. Monaspace rendering
[] as a checkbox), and the only escape hatch was a global
advanced setting.

Add a "Contextual Alternates" toggle to BetterFontPicker's
options menu, sitting alongside the existing stylistic-set
selectors. Unchecking it writes a calt-off feature setting
onto the font descriptor; the setting round-trips through
NSFont.stringValue and fontValueWithLigaturesEnabled: so it
persists per-profile without new plumbing. PTYFontInfo's
it_ligatureLevel returns 1 when the font carries explicit
calt-off, so kCTLigatureAttributeName does not re-enable
calt at draw time.

Always show the calt menu item when ligatures are enabled.
CTFontCopyFeatures only reflects legacy AAT feature data, so
most OpenType fonts don't advertise their calt table even
though they use it; offering the toggle universally avoids
missing the cases users actually care about.

While here, dedupe the stylistic-alt list. AAT type 35
selectors come in On/Off pairs; show only the On halves so
each stylistic set is one toggle rather than two, and apply
the same predicate when reading existing settings off a font
so any Off selectors that snuck in via older pickers or
hand-edited prefs are dropped on the next save.

In addOptionsButton, build the menu before loading state
from the font, so the family-change reset inside
set(familyName:) doesn't clobber the values just read. In
affordance(_:didSelectFontFamily:), reset the options state
before notifying the delegate so the new family doesn't
inherit calt/stylistic-alt selections from the previous one.

2026-05-08: Clarify MRU tab cycle key actions

Add help-button text to Cycle Tabs Forward and Cycle Tabs
Backward explaining the tap-vs-hold-modifier behavior, and
rename their picker group from "Cycle" to "Recent Tabs" to
match the verb+object pattern used by other tab-related
groups.

2026-05-08: Run ssh:// URL and Bonjour profile commands
through user login shell

Use the user's $PATH (from rc files) to resolve ssh, so
Homebrew's ssh and other binaries outside /usr/bin are
found. The SshSchemePath advanced setting still wins when
changed from its default. Issue 3415.

2026-05-08: Adjust cursor X when DECDHL/DECDWL/DECSWL
changes line width mid-line

When ESC #3, #4, or #6 was received with content already on
the line, the existing characters were correctly doubled in
width but the cursor was left at its old buffer column, so
subsequent text overwrote the right halves of the existing
characters. Set the cursor to 2*x (clamped to the right
margin) when expanding to double-width and to x/2 when
compacting back to single-width, matching VT510 spec
behavior.

Co-Authored-By: Claude Opus 4.7 (1M context)
<noreply@anthropic.com>

2026-05-08: Update DeepSeek models to v4-flash/v4-pro and
disable thinking mode (#658)

* feat(ai): update DeepSeek models to v4-flash and v4-pro

- Add deepseek-v4-flash and deepseek-v4-pro models - Remove
deprecated deepseek-chat, deepseek-coder, deepseek-reasoner
- Update API endpoint from /v1/chat/completions to
/chat/completions - Update context window to 1M tokens, max
output to 384K tokens - Set deepseek-v4-flash as recommended
model

* fix(ai): disable thinking mode for DeepSeek v4 models

DeepSeek v4 models enable thinking mode by default, which
requires reasoning_content to be passed back in multi-round
conversations.  Since the codebase does not handle
reasoning_content yet, explicitly disable thinking mode with
thinking: {type: disabled} in requests.

* refactor(ai): address review feedback for DeepSeek v4

- Restore legacy deepseek-chat, deepseek-coder, and
deepseek-reasoner
  model entries with deprecation note (remove after
  2026-07-24)
- Add explanatory comment for thinking: disabled workaround
- Make Thinking.type a typed enum (Thinking.Mode) - Add
release notes entry for DeepSeek v4-flash and v4-pro
2026-05-08: Add windowBorderColor and
windowBorderColorUnfocused (#662)

2026-05-07: Restore tmux modifyOtherKeys state on attach

Query tmux 3.5+'s pane_key_mode format variable when
restoring pane state so that the per-pane modifyOtherKeys
mode (Ext 1 / Ext 2) is re-applied to the iTerm2 session
after a -CC attach. Without this, reattaching while nvim or
another modifyOtherKeys app is running left the session in
Standard key reporting mode.

Also reorder the arrangement-restore code path so the
resetSendModifiers call runs before setTmuxState, not after
otherwise the reset wipes the freshly-restored
sendModifiers[4] value before any keys are typed.

Requires tmux 'extended-keys' option to be 'on' or 'always'
so tmux actually tracks the inner pane state.

2026-05-07: Honor IT2_APP_PATH in Python API AppleScript
cookie request. Issue 12850

External scripts targeting a -suite iTerm2 (no ITERM2_COOKIE
in env) fell through to the AppleScript path, which always
told application "iTerm2"  so the cookie came from whichever
build Launch Services routed to, not the one whose socket
the script connects to, yielding a 401. Mirror the it2 CLI's
convention and let IT2_APP_PATH override the AppleScript
target so callers can disambiguate by path. Bump python
module to 2.18.

2026-05-07: Re-layout content when tab bar accessory
min-height changes. Issue 12811

In Lion fullscreen with Minimal theme and "Hide tab bar when
there is only one tab", adding a second tab made the tab bar
appear over the terminal because the content view was never
re-laid out. The 1->2 transition skipped repositionWidgets
tabBarControl.isHidden returns NO when the control is on
loan to a collapsed titlebar accessory, so the
willShowTabBar predicate misclassified it as "already
visible".

Have the nanny's updateMinHeight report whether the
requested height changed and dispatch_async
repositionWidgets from
updateTabBarControlIsTitlebarAccessory when it does.
Tracking the last requested value in the nanny avoids
reading vc.fullScreenMinHeight, which can be stashed on
probation or overridden with defaultHeight on add.

2026-05-07: Show tab bars during tab/session drags so
single-tab windows accept drops. Issue 12846

2026-05-07: Detect and offer to repair a broken Claude Code
integration

Claude Code periodically rewrites ~/.claude/settings.json
and has been observed dropping our cc-status hook. Add a
one-shot repair prompt that fires the next time claude
launches in a session if the user previously completed setup
but the hook is no longer present (or no longer points at an
executable). A new "Reinstall Claude Code Integration" menu
item gives users a route back into the onboarding panel from
any partial-install state  without it, once anything is
installed the only menu action was Uninstall.

Adds a sticky claudeCodeIntegrationCompleted flag (set by
doInstallHook, cleared by Uninstall) distinct from the disk-
reconciled cache: the cache flips to false when
settings.json loses our entry, but the sticky flag preserves
the "user once opted in" signal that drives the repair
detection. Migrates the flag for users coming from pre-flag
betas (3.7.0beta1/beta2) and pre-flag 3.7 nightlies by
intersecting the recorded version history with the
hook-on-disk state.

GlobalJobMonitor.replayCurrentState lets a late-registering
observer seed itself with current state without depending on
registration order; the new health monitor calls it after
addObserver. Existing observers' handlers are idempotent
against the duplicate notifications.

2026-05-07: Sort Open Quickly sessions by recency. Issue
12799

Each PTYSession now carries a lastActivityOrdinal stamped
from a shared iTermMonotonicCounter on iTermController
whenever the session becomes user-visible: pane changes
within a tab, tab switches, and window switches all bump it.
The Open Quickly model adds a recency contribution strictly
less than 0.01 to each candidate session's score, acting as
a tiebreaker without overriding real query matches.

Both the per-session ordinal and the global counter survive
across launches via the existing app-state restoration paths
(NSCoder and graph encoder).

2026-05-07: Fix tmux send-keys regression: route C0 control
bytes through -H

When the tmux server has extended-keys enabled (3.2+) and a
pane app turns on modifyOtherKeys, tmux 3.5's input_key
narrowed the trivial- case fast path to HT/CR/ESC/printable
only. Other unmodified C0 bytes fall through to
input_key_extended, which requires modifier flags and
returns -1; cmd_send_keys_inject_string then takes the
literal-text fallthrough and writes the original
"0x4"/"0x3"/etc. argument string to the pane.

In the gateway, classify code points 0x00-0x1F as a third
encoding kind that emits "send -H -t %pane NN ..." on tmux
3.0a or newer. -H injects KEYC_LITERAL keys, which input_key
writes as raw bytes regardless of mode. Bytes 0x80+ keep the
existing "0xNN" path so typed Unicode still flows through
tmux's UTF-8 encoder.

Gated strictly on >= 3.0a (NSDecimalNumber 3.01) since tmux
3.0 itself predates the -H flag (added in commit fc2016db,
Jul 2019).

Issue 12845.

2026-05-07: Debounce TTY-based password input detection

Shell scripts that briefly disable terminal echo to suppress
the visible echo of an OSC report response (e.g. it2getvar,
it2profile -g) were tripping the open password manager
automatically feature, since iTerm2's heuristic flips the
moment ECHO turns off and ICANON stays on.

Require ECHO to remain off for at least
DetectPasswordInputDebounce seconds (default 0.2) before
treating it as a password prompt. The conductor's
at-password-prompt signal stays authoritative and is used
immediately. State and timing live in a new
iTermPasswordInputDebouncer Swift class.

2026-05-07: Fix VT100ScreenMark dealloc crash with
unresolved return-code

If returnCodePromise was created but setCode: was never
called (e.g.  session terminated mid-command),
iTermPromiseSeal asserted in dealloc because _promise was
still set. Reject the seal during the marks dealloc, hopping
to the main queue so observers (CommandInfoViewController
uses raw then:) get callbacks on the thread they expect even
when the mark is released on a background queue.

Co-Authored-By: Taylor Jasko <taylor.jasko@konghq.com>

2026-05-07: Hide top-right indicators that overlap a hovered
inline button

2026-05-07: Click anywhere on a folded line to unfold

Extend the existing fold-arrow click handler so that a clean
single click anywhere on a folded lines placeholder unfolds
the block. The margin arrow, right-click menu, and Edit >
Unfold in Selection remain the primary affordances; this is
the wider hit area.

Show the arrow cursor over the whole placeholder line so the
affordance is discoverable. Rename
mouseIsOverFoldButtonInEvent: to
mouseIsOverFoldAffordanceInEvent: since it covers fold
marks, command marks, and now folded-line placeholders.

Drag-to-select, buttons (copy / custom), cmd-click semantic
history, mouse reporting, multi-click word/line selection,
and higher-priority in-line targets (commandMark, pathMark)
are all preserved by their existing higher-priority gates.

2026-05-07: Adjust LineBuffer cursor_x when a partial drop
trims its raw line

dropExcessLinesWithWidth: only updated cursor_rawline for
raw lines it dropped entirely. When the surviving block was
partially trimmed from the front (the path Fold All uses to
keep just the trailing wrapped line), cursor_x kept its old
offset and pointed past the end of the shortened raw line.
getCursorInLastLineWithWidth: then refused to recognize the
cursor, restoreScreenFromLineBuffer: never moved the grid
cursor, and Fold All left it stranded at the bottom of the
grid while the prompt content landed near the top.

LineBlock.dropLines: now optionally reports the partial-trim
offset within the first surviving raw line. LineBuffer
slides cursor_x left by that amount when the cursor lives on
that raw line.

2026-05-07: Add dual-mode SGR underline color (CSI 58:12 /
58:13)

Extends the dual-mode (light/dark) SGR mechanism from issue
12832 to underline color so output produced under one
appearance stays readable after a toggle. Mirrors the 38/48
pattern: a chained colon-form override follows the universal
58:5 / 58:2 fallback so non-supporting terminals still see a
usable color. Issue 12838.

EA serialization round-trips the dark variant in both the
dictionary and binary TLV forms; both are
length/append-elided so EAs without a dark variant are
byte-identical to what older iTerm2 wrote. Renderers
(legacy, Metal, copy-as-attributed-string) resolve
light/dark via a new iTermColorMap helper;
deterministic-by-design call sites (Kitty placement IDs,
Python API gRPC) intentionally keep the light variant.

Also plugs a longstanding gap in DECSC/DECRC and
stateDictionary save/ restore:
dictionaryForGraphicRendition: /
graphicRenditionFromDictionary: now persist hasDualModeFg/Bg
dark variants and the underline dark variant. Promoted to
class methods to make round-tripping testable.  The new keys
are append-only  older state files restore as single-color.

Defensive cleanup: legacy 4-int / 4-element decoders now
zero the dark variant fields explicitly so callers using
stack scratch values can't end up with phantom dual-mode
state. Replaces an unsafe memcmp on VT100TerminalColorValue
(whose padding bytes after BOOL hasDarkVariant could
spuriously differ) with field-by-field equality.

2026-05-07: Flush remote prefs synchronously on quit when
Save Mode is Automatic

The debounced setNeedsSave -> background-queue write could
lose the race against app exit, so a just-suppressed warning
would not make it to the remote plist. On next launch,
copyRemotePrefsToLocalUserDefaults wipes local syncable keys
and copies the stale remote, resurrecting the warning. Issue
12844.

2026-05-07: Require confirmation before closing a pinned tab
(#654)

Adds a confirmation dialog whenever a user attempts to close
a pinned tab, regardless of method (close button,
middle-click, W, or quitting the app). The dialog identifies
the tab as pinned. Each pinned tab is listed by number in
the quit-prompt reason so the user can tell which tabs are
protected.

Co-Authored-By: Eliezer Gonzlez <eliezerghtech@gmail.com>
Co-Authored-By: Claude Opus 4.7 (1M context)
<noreply@anthropic.com>

2026-05-07: Fix enabling Touch ID for sudo on macOS 26+ via
bundled install script. Issue 12753

NSAppleScript admin elevation can no longer write
/etc/pam.d/sudo_local on macOS 26: the elevated process is
TCC-scoped to the calling GUI app, so even cp from a
sudo-with-administrator-privileges shell fails with EPERM.
sudo from Terminal still works.

Replace the AppleScript path with a bundled script
(Resources/install-touchid-sudo.sh) and present two options
when the user accepts the offer:

- Copy Command: writes `sudo
"/path/install-touchid-sudo.sh"` to the
  pasteboard so the user can paste it into any terminal.
- Run In New Window: opens a single-use iTerm2 window
running the
  script. The script prints a banner and a folded block
  containing its own source (via OSC 1337 Block /
  UpdateBlock) so the user has context before sudo prompts
  for a password, then self-execs under sudo.

Track per-invocation dismissal in iTermNaggingController so
the offer stops re-firing while sudo remains the foreground
job, and suppress it entirely in single-use sessions and
whenever lastCommand/commandLine indicates the install
script is already running.

Co-Authored-By: Claude Opus 4.7 (1M context)
<noreply@anthropic.com>

2026-05-07: Request badge authorization

2026-05-05: Add release notes guidelines

2026-05-05: 3.7.0beta2

2026-05-05: Propagate DB errors from DatabaseBackedArray
mutations

removeAll(where:) used try? on the per-element remove, which
left elements unchanged on failure but still recorded the
index as removed and never advanced past it  risking an
infinite loop and a corrupt IndexSet on persistent DB
errors. set(at:) had the same try? anti-pattern, leaving
in-memory state ahead of the DB. Both now propagate errors;
ChatListModel swallows and logs at the one UI call site.

2026-05-05: Convert TwoTextFieldCell from auto layout to
manual layout

2026-05-05: Fix UAF risk in Core Text run enumeration
fallback paths

When CTRunGetGlyphsPtr or CTRunGetStringIndicesPtr return
NULL, we allocate an NSMutableData and hand its bytes to the
closure. The NSMutableData was a strong local with no
further uses after the
.mutableBytes read, so ARC was free to release it before the closure
ran, leaving a dangling pointer. Mark the backing buffers
NS_VALID_UNTIL_END_OF_SCOPE so they survive the closure
call.

2026-05-05: Fix paste-history score epoch inconsistency

2026-05-05: Mark iTermBijection lookups nullable

The header declared objectForLeft: and objectForRight:
nonnull (under NS_ASSUME_NONNULL_BEGIN), but both return nil
for missing keys. Without an Optional context Swift imported
the methods as non-Optional, leaving the result variable
holding nil under a type that claims it can't be.

2026-05-05: Remove stray en-space append in git rich-stats
path

2026-05-05: Fix retain-cycle leaks in DLL-backed caches and
LRU bump bug

The doubly-linked list used by iTermCache and Swift
LRUDictionary had both prev and next as strong references,
leaking every cached entry once the owning cache was dropped
(the parallel dictionary's release wasn't enough  adjacent
nodes retained each other). Make the back-pointer weak in
both implementations; the dict and forward chain still keep
every node reachable.

Also fix LRUEvictionPolicy.bump, which removed and
re-appended a node but left nodesByKey pointing at the
detached old node. A later re-insert or delete then
corrupted list head/tail and changed which element was
treated as LRU.

Adds regression tests for both leaks and the bump bug.

2026-05-05: Fix \r handling in TmuxGateway octal escape
decoder

A \r byte injected mid-octal-sequence was skipped via
continue, but the loop counter still advanced, so only 2 of
the 3 octal digits got read and the decoded byte was wrong.

2026-05-05: Fix bitwise & typo in iTermSessionLauncher
retain-cycle check

Use logical && in maybeBreakRetainCycle. Also drop a dead
macOS 11 availability check around loadDeferredURLIfNeeded
(deployment target is macOS 12).

2026-05-05: Fix bug in detecting old iTerm as default
handler for a sheme

2026-05-05: Fix mislabeled DLog in ChatCellView
typing-stopped branch

The else branch was logging "set typing=true in \(false)"
copy-paste of the sibling log line. Correct it to report
typing=false and interpolate the chatID.

2026-05-05: Fix annotation cursor jumping to end while
typing

Reassigning NSTextView.string resets the selection to the
end. The optimistic main-thread write in
VT100Screen.setStringValueOfAnnotation:to: echoed back
through annotationStringDidChange: and reassigned the text
view's string after every keystroke, kicking the caret. Skip
the reassignment when the value is unchanged.

2026-05-05: Fix dangling-target crash in Undo after closing
a split pane

NSTextView with allowsUndo=YES registers
_undoRedoTextOperation: actions on the responder chain's
undo manager, which for these views was the window's.
NSUndoManager retains its action targets unowned(unsafe), so
when a split pane was closed the note, session note, or chat
input text views inside it were deallocated while their
action registrations stayed in the window's undo stack as
dangling pointers. The next EditUndo (or Z) sent
_undoRedoTextOperation: to freed memory and crashed in
objc_msgSend.

Same bug also hijacked the menu title: the window's undo
manager reported canUndo=YES because of the stale
registration, so iTermApplicationDelegate's
responderForMenuItem returned the window and the item stayed
labeled "Undo" / "Undo Typing" instead of falling through to
"Undo Close Session".

Each of the three text views now provides its own
NSUndoManager (via undoManagerForTextView: on
PTYNoteViewController, and an override of undoManager on
SessionNoteTextView and ChatInputTextView). The undo stack's
lifetime is now bound to the text view's, so a closed pane
can't leave registrations behind.

2026-05-05: Fix graph db recovery rowid leak

The recovery path in trySaveEncoder aliased the failed
save's tree into a fresh recovery encoder and then called
eraseRowIDs in place, mutating record instances that other
code (notably the previous-revision tree, via the
unchanged-generation alias branch in iTermGraphDeltaEncoder)
still held. Combined with iTermOrderedDictionary
byMapping:'s "first occurrence wins" deduplication in
reallySave's enumerate, that left some records with
rowid=nil permanently in the published self.record. The next
save then tripped MissingRowID at iTermGraphDatabase.m:376.

Recovery now deep-copies originalEncoder.record into a tree
it owns exclusively (no shared instances), with rowid=nil
throughout and siblings deduplicated by (key, identifier) so
reallySave's INSERT pass assigns a fresh rowid to every
record.

2026-05-05: Add failing test for graph db recovery bug

2026-05-05: Convert AITerm chat UI from auto-layout to
manual layout

Replaces the chat tree's NSStackView + constraint cascade
with a manual-layout helper (iTermChatManualStackView) and
per-cell layout() overrides. The chat input view,
controller, toolbar, and every message cell now compute
their own frames; row heights come from static cellHeight
helpers instead of fittingSize on a prototype cell. The
motivation is to break the auto-layout coupling that linked
the chat input field's internal scroll view back to the rest
of the tree, which was triggering AppKit's "more passes than
views" guard during streaming updates and causing layout
thrash on macOS 26.

Drops the macOS 26 NSGlassEffectView wrappers in favor of
plain NSVisualEffectView so neither the chat input container
nor the floating toolbar bar pulls in the glass view's
auto-layout subtree.

Plumbs the inline-chat link flow on PTYSession so the gutter
chat receives the same "linked to session" notice and
permissions buttons the standalone chat window already gets.

ChatService.setPermissions now applies synchronously instead
of routing through agentWorking; the typing-status pulse was
making the chat scroll on every permission button click.

Two correctness fixes folded in:

- MultipartMessageCellView.measuredHeight for
.codeAttachment
  (and the static cellHeight path) measure at the same
  effective width that layoutSubpart will actually set on
  the text container. Without this the bubble was sized for
  fewer wrap lines than the live render produced, so the
  bottom of long code blocks was clipped and the bubble sat
  at the bottom of an over-tall row.
- scrollToBottom forces the manual layout pass to run before
  reading scrollView.contentInsets.bottom. The synchronous
  scrollToBottom inside load(chatID:) ran before the layout
  joiner had fired, so contentInsets.bottom was 0 and the
  setBoundsOrigin target was clamped to the wrong place,
  leaving the last bubbles obscured by the input view.

2026-05-05: Add advanced setting to inject IT2_APP_PATH

When multiple iTerm2 builds with the same bundle identifier
are running, AppleScript tell application \"iTerm2\" can
resolve to the wrong instance, so the it2 CLI ends up
requesting a cookie from one iTerm and connecting to
anothers socket. The new setting injects IT2_APP_PATH on
session start so it2 targets this specific build via
AppleScript. Off by default; primarily useful for iTerm2
developers.

Co-Authored-By: Claude Opus 4.7 (1M context)
<noreply@anthropic.com>

2026-05-05: Add chat as right gutter view

2026-05-05: Fix multi-second hang when closing Settings
window

Each PreferenceInfo registered for
kPreferencePanelDidLoadNotification in -init and removed
itself in -dealloc. Closing the panel cleared the keyMap,
deallocating thousands of PreferenceInfos and producing
O(N^2) work in CoreFoundation's notification registrar
removal.

Register only when an observer block is set (and unregister
when it is cleared), so the registrar list stays small and
teardown is cheap.

2026-05-04: Refresh stale session hostname variable on first
OSC RemoteHost

didFinishInitialization seeds
iTermVariableKeySessionHostname from gethostname(), which
can be a transient DHCP name (AT&T's gateway hands out bare
"Mac" via option 12). The OSC RemoteHost handler only fired
screenCurrentHostDidChange when the new mark differed from
the interval tree's last-line host, so when the shell's
later OSC matched what restoration had already parked in the
tree, the main-thread hostname variable kept the stale
init-time value forever.

Track the last hostname pushed to the delegate on the
mutation side and also fire when the new value differs from
that. First OSC of a session always pushes once;
steady-state repeats remain no-ops, so no extra pause
mid-prompt.

2026-05-04: Move OpenDirectory user-shell lookup off the
main thread

Diagnosed from a sample where 100% of main-thread time
(3865/3865 samples) was blocked in +[iTermOpenDirectory
userShell] opendirectoryd via a Run Command trigger firing
on every cadence tick. The OD query is now hosted in the
pidinfo XPC service (wrapped in performRiskyBlock so a
wedged daemon is detected and recovered from instead of
burning a thread in the main app), and the main-app entry
point caches the result behind an iTermPromise.

Layout: - iTermOpenDirectory.{h,m} declares only
+performBlockingLookup,
  built into both the main app and pidinfo. Pidinfo links
  OpenDirectory.framework and calls it from inside its
  watchdog.
- iTermOpenDirectory+MainApp.{h,m} adds the (MainApp)
category
  with +userShell and +prime, built into the main app only.
  Each call kicks a fresh background lookup via the slow-op
  gateway and waits up to 20ms for it; if it doesn't return
  in time the cached value from the previous successful
  query is returned.  First call (no cache yet) blocks until
  the lookup settles.
- iTermSlowOperationGateway gains
fetchUserShellWithCompletion:,
  which always invokes its completion exactly once on a
  non-main queue (avoiding deadlock when a main-thread
  caller is blocked in promise wait) with a 15s
  defense-in-depth timeout for the case where an XPC
  connection drops mid-call.
- +prime is called from applicationWillFinishLaunching: with
a
  self-rescheduling catchError so the cache is populated
  before any synchronous caller needs to block.

The closure-vs-assignment race in the previous draft is
gone: gCurrentLookup is never written from the gateway
completion; inFlightLookupLocked rotates the promise
synchronously when the old one has settled (hasValue covers
both fulfill and reject).

2026-05-04: Add parameter to Copy smart selection action

Lets users specify an interpolated string to copy instead of
the matched text. Empty parameter preserves the original
behavior of copying the whole match. Also fixes a
long-standing bug where picking the Copy action from the
right-click context menu was a no-op because
contextMenuActionCopy: only handled URLAction senders
(cmd-click) and ignored NSMenuItem senders.

2026-05-04: Restore SmartSelectionController delegate wiring

The May 2025 smart selection UI revamp dropped the delegate
outlet, so edits made via the smart selection editor stopped
flushing the profile or posting kReloadAllProfiles, and
running sessions kept stale rules until the settings panel
was reopened. Re-wire the outlet and guard setGuid: against
redundant reloads so live edits no longer lose the table
selection.

2026-05-02: Order announcements below the per-session
toolbar in z-order

Announcements were inserted with addSubviewBelowFindView,
putting them at the top of the SessionView's z-stack. The
slide-in/slide-out animation starts above the final
position, so the announcement painted over the toolbar while
moving.

2026-05-02: Expose iterm2.appBundlePath and use it in the
Claude Code workgroup template

Replaces the build-time Bundle.main.path lookup and inline
JSON in ClaudeCodeWorkgroupTemplate with runtime
swifty-string interpolation of the new global, and moves the
--settings JSON into a bundled code-review-settings.txt
resource. Path interpolations are wrapped in single quotes
so installs at paths with spaces still work.

2026-05-02: Rename claude code peer to chat

2026-05-02: Tell CLAUDE.md about NoSync

2026-05-02: Plumb a workgroup-wide git base ref through the
diff pipeline

The git-base selector publishes its picks as the workgroup's
currentGitBase, owned by the instance and mirrored into
every peer port's leaderScope (as the `gitBase` swifty
variable) and every sibling selector. A pick triggers: a
fan-out to all selectors so siblings reflect the new value,
a poller refresh against the new base, and a restart of
every diff session (peer + non-peer) with commands
re-resolved through resolvedCommand/resolvedPerFileCommand.
The pidinfo XPC layer (PIDInfoGetGitState + iTermGitState +
iTermSlowOperationGateway + iTermGitPollWorker +
iTermGitPoller) now carries an optional gitBase string end
to end; nil/HEAD keeps the legacy git-status pass, anything
else hits the new populateFileStatusesAgainstBase: path that
diffs base^{tree} against the working tree (with index
applied) and routes every delta through workdirStatus so the
picker shows one flat list when the comparison point isn't
HEAD.

2026-05-02: Grow git-base selector to fit live edit text

Width tracks the longest dropdown entry plus the live editor
contents (folded in on every keystroke via
controlTextDidChange so the toolbar relays out as the user
types past the widest persisted recent), with chrome budget
bumped to give the caret room past the dropdown chevron.
Also moves dropdown rebuilds from inside commit()  which
crashed via a re-entrant removeAllItems  selection-change
cascade  into a lazy comboBoxWillPopUp refresh, suppresses
no-op delegate fires via lastCommittedBase, adds a
displayBase entry point for sibling-selector mirroring
without yanking a mid-typing user, and caps recents-entry
length so a stray paste can't bloat defaults.

2026-05-02: Silence two warnings from the Code Review
overlay

Move
presentCodeReviewPromptOverlayWithDefaultPrompt:onStart:
into a SessionView category so the main @implementation no
longer triggers -Wincomplete-implementation, and capture the
prompt view weakly in onStart instead of binding an unused
weak local.

2026-05-01: Fix some nonsense broken python twine BS

2026-05-01: Add App.async_apply_layout Python API for atomic
tab/pane layout changes

Reshape one or more tabs in a single transaction: swap
panes, change splitter orientation, restructure nested
splits, move sessions across tabs and windows, close
sessions/tabs/windows. Validation runs up front so malformed
specs are rejected before any mutation. Includes Swift unit
tests for parser/validator/resolver/transaction and 47
Python integration tests against a live iTerm2 process.

2026-05-01: Save multiple Code Review prompts with a
CRUD-managed list

Replaces the single Code Review prompt previously managed in
Settings > General > AI > Prompts with a named collection.
The overlay grows a Prompts pulldown for quick load + Save
Current as New; a dedicated Manage Prompts window built on
CRUD.swift handles add/rename/edit/reorder/delete. The
legacy preference seeds a Default entry on first launch.

2026-05-01: Add nullability annotations to PTYSession.h and
PSMTabBar headers

Wrap PTYSession.h, PSMTabBarControl.h, and PSMTabStyle.h in
NS_ASSUME_NONNULL_BEGIN/END and mark genuinely-nullable
members. Notable changes:

- PTYSession.profile is nullable (unset between
-initSynthetic: and the
  first -setProfile:); add a -justProfile accessor that
  returns @{} for Swift call sites doing read-only key
  lookups.
-
PTYSession.{view,textview,delegate,realParentWindow,parentWindow,tty,
  badgeLabel,backspaceData,hostnameToShell,environment,contentSubscribers,
  processInfoProvider,effective{,Un}processedBackgroundColor,apsContext,
  processedBackgroundColor,...} are nullable to match their
  actual return-nil paths.
- iTermRightGutterController guards on session.view as well
as the
  profile so a nil view no longer triggers a spurious layout
  cascade via the optional-vs-non-optional CGFloat
  comparison.
- Workgroup peer spawn rejects the promise when the parent
session has
  no profile or no view, instead of silently sizing the peer
  to .zero.
- iTermRenderingComparer keeps the ?? .black fallback for
  processedBackgroundColor; the renderer skips the fill on
  nil and that would leave margins transparent in the
  comparer's pixel diffs.
- Update Swift call sites in PTYSession.swift,
PTYSession+Browser.swift,
  PseudoTerminal.swift, and
  GetProfilePropertyBuiltInFunction.swift to use justProfile
  for read-only profile lookups.
- ModernTests: drop now-redundant force-unwraps of s.screen
and
  tempSession.guid (formerly IUOs, now strict non-optional)
  and add nil-guards in TerminalRenderer for session.view /
  session.textview.

2026-05-01: Fix shell-arg truncation and POSIX/bash quoting
bugs

componentsInShellCommand was running every unquoted argv
element through -stringByExpandingTildeInPath, which
silently caps at PATH_MAX (1024). Long Code Review launches
died with zsh "unmatched '" because the trailing
prompt+flags got lopped off.  Now only the leading ~ or
~user component is expanded; the rest of the argument is
left intact.

Also fix
-escapeShellCharactersWithSingleQuotesIncludingNewlines:
forBash:. The POSIX '...' path was emitting unparseable
'a\'b' for embedded apostrophes and injecting stray
backslashes before literal CR/LF. The bash $'...' path is
reworked to use ANSI-C escapes (\n, \r, \x27) so control
chars and apostrophes survive the round-trip.

Normalize CR/CRLF to LF in the Code Review prompt wrapper so
stray carriage returns from NSTextView or pasted content
don't turn into line-continuations after backslash escaping.

2026-05-01: Diff nav improvements

2026-05-01: Show changed-file count in workgroup diff
selectors All Files row

2026-05-01: Add Exit Workgroup menu item to Shell >
Workgroups

The submenu now carries a static separator + Exit Workgroup
item from the xib; dynamic workgroup entries are inserted
above the separator, and the separator hides when no
workgroups are configured. Exit Workgroup is enabled only
when the current session belongs to a workgroup.

2026-05-01: Add release note for Code Review overlay layout
fix

2026-05-01: Move Code Review prompt overlay ownership into
SessionView

The overlay's frame is derived from scrollview.frame, but
its observers only fire when the SessionView itself resizes.
Toolbar and title-strip changes resize the scrollview
without changing the SessionView, so the overlay stayed at
the old size and ended up tucked under the toolbar after a
vertical split or peer swap.

Hook updateCodeReviewPromptOverlayFrame into -[SessionView
updateLayout] so any layout-affecting change re-syncs the
overlay. While here, move overlay creation from PTYSession
into a SessionView Swift extension and keep a weak property
for it  outside callers no longer reach into
sessionView.subviews to add or remove the prompt.

2026-05-01: Add release note for announcement-overlap fix

2026-05-01: Position announcement below the reserved title
slot

The announcement frame formula skipped titleHeight on the
theory that hiding the title strip frees its space (issue
6981). That predates the workgroup toolbar, whose layout
always reserves the title slot  so once a SessionView shows
both, the announcement landed inside the toolbar instead of
above the title.

Subtract both the toolbar and the title reserved heights,
and stop toggling the title's hidden state when
announcements appear.

2026-05-01: Find non-visible workgroup peers in API/reveal
callers; preserve maximize across peer swaps

Replace -sessionWithGUID: (tabs only) with
-anySessionWithGUID: (also reaches buried sessions and
workgroup peers held by an in-tab session's peer port) at
every call site that should accept non-visible peers:

-
iTermAPIHelper.sessionForAPIIdentifier:includeBuriedSessions:
the
  gatekeeper for every Python API session lookup. Workgroup
  peers can fail to register with iTermBuriedSessions
  (addBuriedSession early-returns when
  restorableSessionForSession is nil), so without this they
  hit "No such session" before the registered builtin's body
  runs.
- URL handlers ending in [session reveal]: revealMark,
  revealAnnotation, revealLocation, GlobalSearch result
  click.
- Session-state builtins: set_status, add_clipping,
  get_profile_property, focus.
- AITerm chat lookups that gate buttons or call reveal() on
  session existence.

FocusBuiltInFunction now calls reveal() before takeFocus().
takeFocus alone is a no-op for non-visible peers and buried
sessions (their first responder target isn't in any window),
so "focus" via the API was a lie for those cases.

PTYTab swapSession:withBuriedSession: unmaximizes before the
swap and re-maximizes after. Previously, swapping peers on a
maximized tab mutated the off-tree stash splitter (idMap_
holds non-visible peers' SessionViews disconnected from any
view hierarchy), corrupting the saved layout and failing
-unmaximize's "1 subview" invariant later  surfacing as an
assertion crash during quit teardown when removeSession
fired unmaximize. Re-maximizing after keeps the swap
transparent: the user's maximized layout persists, with the
new peer in the maximized slot.
