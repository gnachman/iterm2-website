- Fixes a crash on OS 10.10 when the prefs panel is closed.
- Improves the appearance of diagonal stripes when broadcasting input. They now
  stay put.
- Improve support for sending backslash on Japenese keyboards when the Yen key
  is pressed.
- Fix a bug where iTerm2 would hang if a profile's command failed immediately.
- When a profile's command cannot be run, print the error to the session.

SHA-256 of the zip file is
76f31f4a49e8e6d2a5052fd99049a17b3e832f55a129e81c562958ba822317b3
